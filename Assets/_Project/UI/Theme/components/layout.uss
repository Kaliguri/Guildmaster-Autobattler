/* Явное объявление «здесь переполнение обрезается» для коробок вне тела панели. Владелец обрезки
   один — этот класс, поэтому и разметка, и гейт раскладки (ScreenLayoutOverlapTests) читают
   намерение из одного места. */
.gm-clip {
    overflow: hidden;
}
