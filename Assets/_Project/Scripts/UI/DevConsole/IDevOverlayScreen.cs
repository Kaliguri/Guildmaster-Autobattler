namespace Guildmaster.UI.DevConsole
{
    /// <summary>
    /// Маркер dev-полки (командная консоль, лог, витрина боёв). Такие экраны **взаимоисключающи**:
    /// открытие одного снимает остальные.
    /// </summary>
    /// <remarks>
    /// Нужен как раз потому, что показывают их РАЗНЫЕ владельцы: консоли — <c>MenuRouter</c> (UI-слой),
    /// витрину боёв — dev-модуль (он знает про контент, UI-слой про бои знать не должен). Без общего
    /// признака ни один из них не мог бы снять чужую полку, и две полки наезжали друг на друга.
    /// Маркер даёт ровно столько знания, сколько нужно: «это dev-полка», без типов и подсистем.
    /// </remarks>
    public interface IDevOverlayScreen
    {
    }
}
