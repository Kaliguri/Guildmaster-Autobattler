namespace Guildmaster.Core.Persistence
{
    /// <summary>
    /// Хранилище <b>машинно-локальных</b> данных: тот же контракт, что у <see cref="ISaveService"/>, но
    /// другой каталог — вне <c>Saves/</c>, то есть <b>вне маски Steam Cloud</b> (ТЗ [[save-system]] §3–4).
    /// <para>Сюда кладётся то, что описывает не игрока, а его компьютер: разрешение, режим окна, частота
    /// обновления. Синхронизировать это между машинами нельзя — второй ПК получил бы разрешение первого
    /// и, в худшем случае, чёрный экран на неподдерживаемом режиме.</para>
    /// <para>Отдельный интерфейс, а не флаг в вызове: у двух хранилищ разная политика синхронизации, и
    /// перепутать их должно быть невозможно на уровне типа, а не по внимательности.</para>
    /// </summary>
    public interface ILocalSaveService : ISaveService
    {
    }
}
