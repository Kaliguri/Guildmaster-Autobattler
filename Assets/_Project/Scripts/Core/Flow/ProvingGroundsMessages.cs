namespace Guildmaster.Core.Flow
{
    /// <summary>
    /// ИНТЕНТ «уйти на Ристалище» — площадку вне забега (ГДД «Modes - Proving Grounds»). Шлёт dev-команда
    /// или кнопка; исполняет верхний цикл игры.
    /// </summary>
    /// <remarks>
    /// Отдельное сообщение, а не прямой вызов: главное меню — экран, ЖДУЩИЙ выбора игрока, поэтому
    /// площадка не может просто открыться под ним. Запрос завершает это ожидание тем же исходом, что дала
    /// бы кнопка меню, а если меню сейчас не на экране (идёт забег) — запоминается и срабатывает, как
    /// только цикл в меню вернётся. Живёт в <c>Core</c>, потому что его шлёт и UI, и dev-слой.
    /// </remarks>
    public readonly struct OpenProvingGroundsRequest
    {
    }
}
