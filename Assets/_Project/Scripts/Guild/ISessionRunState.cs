namespace Guildmaster.Guild
{
    /// <summary>
    /// Состояние забега ТЕКУЩЕГО СЕАНСА: то, что регистрирует в своём составе Сессия. Контракт тот же,
    /// что у <see cref="IRunStateView"/>, — отдельно только имя.
    /// </summary>
    /// <remarks>
    /// <b>Зачем второе имя одному контракту</b> — ровно затем же, зачем оно у команд
    /// (<c>Guild.Commands.ISessionRunCommands</c>): снаружи под <see cref="IRunStateView"/> стоит вечный
    /// роутер, и спроси он свой же интерфейс, резолв ушёл бы вверх по контейнеру и вернулся к нему
    /// самому.
    /// <para><b>Реализаций две, и выбирает состав:</b> у владельца это <see cref="RunStateService"/>,
    /// который забег держит, грузит и пишет; у гостя — приёмник снимков, который умеет только читать
    /// присланное.</para>
    /// </remarks>
    public interface ISessionRunState : IRunStateView
    {
    }
}
