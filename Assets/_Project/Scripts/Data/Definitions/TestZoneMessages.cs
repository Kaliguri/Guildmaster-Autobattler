namespace Guildmaster.Data.Definitions
{
    /// <summary>
    /// Тумблер тест-зоны (QA #2): «Бой» вне реального боя → войти/выйти из расстановки отряда на серой
    /// тест-арене (без врагов — полигон для расстановки/реликвий). Публикует топбар (UI), слушает фаза
    /// расстановки в боевом скоупе. Пустой сигнал — состояние (вошли/нет) держит слушатель.
    /// </summary>
    public readonly struct ToggleTestZoneRequest { }
}
