# Ночной заход: поиск и починка мелких дефектов

Начат 2026-08-03, ветка `dev`. Мандат Макса: искать мелкие баги и проблемы, чинить автономно,
каждый фикс — свой коммит, мелкий рефакторинг разрешён, полный EditMode один раз в конце.

**Границы (согласованы до старта):**

- Правим только `.cs`. Ассеты, SO, сцены, префабы — не трогаем.
- Не режем «мёртвый» код: HARD-правило требует сверки с ГДД, «ноль ссылок» ещё не мёртвое.
- Не меняем баланс-числа и не ослабляем тесты.
- Всё, что требует вкуса или взгляда в игре, уезжает в отчёт, а не в коммит.
- Пуш не делаем — коммиты остаются локально в `dev`.

## Разрез кодовой базы

| Кластер | Файлов | Строк |
|---|---|---|
| Combat | 120 | 16452 |
| Presentation | 77 | 15827 |
| EditorTools | 45 | 10299 |
| Game | 90 | 10462 |
| UI | 47 | 8340 |
| Data | 89 | 7156 |
| Balance | 28 | 4012 |
| Net | 27 | 3541 |
| Core | 45 | 3016 |
| DevTools | 12 | 2744 |
| Guild | 30 | 2309 |

## Ход захода

- [x] Границы согласованы, журнал заведён.
- [ ] Волна 1 аудита: Combat, Presentation, Core+Net, Data.
- [ ] Волна 2 аудита: Game+Guild, UI, Balance+DevTools+EditorTools.
- [ ] Верификация находок, починка, коммиты.
- [ ] Полный EditMode.
- [ ] Отчёт.

## Находки и что с ними стало

### Починено

| Что | Где | Коммит |
|---|---|---|
| `PreventDefault` устарел; заменяется парой, а не одним `StopPropagation` | DevConsoleScreen, DevBattleBrowserScreen | `48ff070e0` |
| Устаревшая перегрузка `FindObjectsByType` с `FindObjectsSortMode` | DevCommandTool | `f8d50e49e` |
| `Nbsp` записан живым U+00A0 против обещания собственного докстринга | StatFormat | `8d4f89016` |
| `ResourceRegenFlat` не объявлен `PerSecond` — показ в чужой размерности | StatKinds + тест | `2ba099c4f` |
| Инструмент: сводка предупреждений компилятора по кодам | compile-check.ps1 | `32ffee9ae` |

| Расстановка: превью и постановка проверяли РАЗНЫЕ зоны — противника на Ристалище нельзя переставить | DeploymentController | `e25a952ee` |
| Гейт согласия: снимался один ключ из двух — делегат мёртвого контроллера жил дальше | DeploymentController | `decc31d0f` |
| Dev-разрез текстового события открывал занятие и не закрывал | GameFlow | `8a3188143` |
| Перезапуск акта списывался, когда исполнить его некому | BattleFlow + IBattleSession | `3f4ac2859` |
| Отпускание указателя съедалось глушением — боец залипал «в руке» | InputService + DeploymentController | `1eab38e9c` |
| Пропуск слота ростера молчал, хотя ломает индексы гильдии | GuildRoster | `a1deb1524` |
| Откат добавленного модификатора писал NaN вместо снятия записи | ContentEditService/Batch | `22ec44c2c` |
| Обратный пресет писался и для пропущенных правок | ContentEditBatch | `873b98f47` |
| Правка стата с дублями молча доставалась не тому модификатору | ContentEditService | `b1042785e` |
| Справочник баланса звал операции именами, которых в коде нет | edit-toolkit.md | `56dd27ab9` |
| Дев-консоль и лог-экран мертвы после первого закрытия (подписка в Build) | DevConsoleScreen, DevLogScreen | `8a397ad1d` |
| Свёртка тегов работала только для первой карточки | LoadoutInventoryView | `7aa044b1d` |
| Подписи слайдеров громкости мимо локализации | MenuRouter | `6246e7c4b` |
| Список «что сбросить» показывал сырые id вместо имён | RewardScreenView | `5503ce35a` |
| Перебор цепочки тултипов с правкой её же внутри | TooltipSystem | `fd55546a9` |
| Топбар брал счёт ЛЮБОГО гейта готовности | UiRootBootstrap + общие ReadyKeys | `46811e6d7` |
| Залипание тултипа применялось дважды — двойные колбэки на ссылках | TooltipSystem | `ed935a839` |
| Приёмник лога подключался и не отключался | MenuRouter | `72c7b9b47` |
| Отменённый токен оставлял навигатору вечную регистрацию | UiNavigator | `b96740152` |
| Экран, резолвнутый в Build, всё равно попадал в стек | UiNavigator + UiScreen | `9bcd82883` |
| README числил NGO, Easy Save, Feel, MPPM и Quantum Console — ни одного в проекте нет | README | `148cb2e54` |
| Тесты на два HARD-правила: корень меню и невидимые символы | ProjectConventionTests | `051a8e13a` |

### Проверено и не подтвердилось

_Пока пусто._

### В отчёт Максу — решать ему

- **Забег недетерминирован.** `RunState.Seed` кормит только карту акта и сид боя. Награды, лавка,
  ролл «?» и события берут корневой `IRngService`, посеянный `DateTime.UtcNow.Ticks` и никогда не
  пересеваемый от забега. Выйти в меню и продолжить из автосейва = реролл витрины и полки магазина.
  Докстринги `RewardService` и `NodeResolver` при этом обещают детерминизм «для реплея/коопа».
  Чинится переносом генератора забега в скоуп сессии — но это решение про кооп и реплей, не моё.
- **Передышка сносит боевой скоуп раньше, чем показывает свои кнопки.** `RunBeatStage` зовёт
  `RequestReset` → `BattleHost.Close`, а единственный подписчик «К построению» и таба «Бой» живёт
  в убитом скоупе. Между узлами обе кнопки молча не делают ничего. Развилка: не сносить скоуп на
  входе в передышку либо поднять владельца расстановки на уровень занятия.
- **Термины не получают `desc.full` и падежные формы при авторинге.** `RequiredSuffixes` уводит домен
  `kw` в общую ветку «name + desc», хотя рантайм читает и полное описание, и падежи, а докстринг
  `KeywordDefinition` обещает, что оба описания заводятся сразу. Починить ночью нельзя: `ContentValidationTests`
  требует, чтобы обязательные ключи уже существовали в таблице, а таблицы правятся только через
  открытый редактор — правка YAML мимо Unity теряется. Нужен твой заход: завести ключи и дописать ветку.
- **Пустая сборка `Guildmaster.MiniGames`.** Ни одного `.cs`, но `Guildmaster.Game` на неё ссылается.
  Каждый прогон компиляции спотыкается о неё («сборка незнакома редактору»). Резать не стала — правило
  требует сверки с ГДД, а мини-игры могут быть в замысле.

## Очередь находок аудита

**Data (принято от аудитора, проверяю по одной):**

- [x] `Nbsp` живым символом — починено.
- [x] `ResourceRegenFlat` мимо таблицы размерностей — починено.
- [ ] Откат `setStat` пишет NaN, когда правка ДОБАВИЛА модификатор (ContentEditService/ContentEditBatch).
- [ ] Обратный пресет пишется и для пропущенных правок — две копии проверки разошлись.
- [ ] `SetStat` правит только первое совпадение по стату.
- [ ] Авторинг ключей не заводит `desc.full` и падежные формы для домена `kw`.
