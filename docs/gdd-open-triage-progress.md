# Журнал захода: расчистка `open.md` и статусы контента

Заход начат **2026-07-29**. Постановка — Макс: (1) вынести решённое из открытых вопросов в журнал,
(2) разобрать черновые идеи и разнести по канону, (3) завести разделители «реализовано / теория» и
док-статус на каждый тип контента.

Порядок согласован: **структура → расчистка → анализ черновиков**.
Идеи фиксируются только после вердикта Макса (правило скилла: своё предложение — `proposed`).

---

## Фаза 1 — структура (СДЕЛАНО 2026-07-29)

Итог: 67 карточек получили `impl`/`asset`/`order` по полосам; заведены 5 разделителей и 4 док-статуса;
линт расширен под новые поля; конвенция описана в справочнике скилла. Оба гейта зелёные
(`check-wiki-frontmatter` 179 доков, `check-wiki-links` 206 доков).

Что делаем: сквозной `order` по группам, файл-разделитель в каждой папке контента,
`implementation-status.md` на тип контента, поля `impl` / `asset` во frontmatter карточек.

### Собранные факты (сверка ассетов с ГДД, 2026-07-29)

Мерка: наличие ассета в `Assets/_Project/ScriptableObjects/` (для эффектов — рантайм-компонента
в `Assets/_Project/Scripts/Combat/Effects/Components/`).

**Реликвии — 10 из 21 карточки в движке:**

| Ассет | Карточка |
|---|---|
| `relic.assassin` | the-verdict |
| `relic.cryomancer` | the-winter |
| `relic.defender` | the-bulwark |
| `relic.druid` | the-bloom |
| `relic.flame_swordsman` | the-pyre |
| `relic.iron_spearman` | the-spear |
| `relic.light_shepherd` | the-shepherd |
| `relic.ranger` | the-hunter |
| `relic.treant` | the-thorn |
| `relic.whirl_monk` | the-gale |

`relic.base` — служебный базовый ассет, карточки не имеет и не должен.
Без ассета (11): the-bond, the-bonewright, the-cadence, the-draugr, the-martyr, the-mirror,
the-paragon, the-runesmith, the-storm, the-tide, the-warden.

**Враги — 4 из 13:** `enemy.goblin_archer`, `enemy.goblin_cutthroat`, `enemy.goblin_grunt`,
`enemy.goblin_warrior`. Плюс `enemy.training_dummy` — дев-болванка, карточки не имеет.
Без ассета: goblin-commander, goblin-shaman, goblin-wolfrider, pack-wolf, earth-golem и все
четыре бандита.

**Виды:** в движке только `Goblins.asset`; bandits / beasts / golems — только доки.

**Предметы — расхождение в обе стороны:**

| Ассет | Что внутри | В ГДД |
|---|---|---|
| `item.swift_boots` | `Stat 20`, Op Flat, `0.6` | есть в common-items, но там **+30%**, а в ассете flat |
| `item.oaken_charm` | MaxHp +200, Vessel | **карточки нет** |
| `item.war_banner` | MaxHp +80, Party | **карточки нет**; в ГДД четыре других Знамени |

**Эффекты — по наличию рантайм-компонента.** Нет в коде вовсе: Bleed, Sleep, Charm, Frenzy,
Immobilize, Silence. Есть: Burn (Ember/Ignition), Shield, Stealth, Mark, Heal, Dispel,
StatModifier, Displace, Control (стан). Яд — есть тег и урон, идентичность (анти-хил) нет.

### Решения по структуре (мои, механика — не дизайн)

1. `impl: engine | partial | paper` во frontmatter карточки — **единственный владелец** факта
   «в каком состоянии». `asset: [id]` — чей ассет. `impl_note` — чем именно расходится.
2. `order` по группам: служебные 0–9 · в движке 10–499 · **разделитель 500** · теория 510+ ·
   сырьё-инкубаторы 900.
3. Разделитель — файл `divider-theory.md`, `title` с обязательным кластер-префиксом (иначе
   роняет `check-wiki-frontmatter.ps1`, ослаблять гейт не стал).
4. `implementation-status.md` в каждой папке контента — сводка (Dataview) + раздел про то, чего
   в ГДД нет вовсе (движковые заглушки).

### Долг, найденный по ходу

- Док-строка `check-wiki-frontmatter.ps1` обещает пропуск `template-*`, а код их проверяет.
  Расхождение док↔код, чинится правкой док-строки.
- `item.oaken_charm` и `item.war_banner` — контент в движке без дизайна. Решить: завести карточки
  или удалить ассеты (вердикт Макса).

---

## Фаза 2 — расчистка `open.md` (СДЕЛАНО 2026-07-29)

`open.md` переписан на три раздела: развилки без вердикта · сырьё Макса под разбор · инкубатор
требований. Что куда ушло:

| Было в open | Куда уехало |
|---|---|
| 15 закрытых развилок с разборами (ГД на поле, заклинания ГД, тайминг испытания, защита от невезения, A1, A3, B1, C4, D1–D6) | новый [[gdd/00-meta/closed-forks]] — приложение к журналу |
| три сырые заметки с врезками ПРИНЯТО (Судьба · классы HP/MS · DPS-коридоры) | удалены: канон в `stats` и `vessel-progression` сверен построчно, причина в журнале |
| Блок E «риски жанра» | снят как дубль — владелец roadmap §Блок 4 |
| B2 «обучение AI по реплею» | снят как дубль — владелец roadmap §Блок 3 |
| сырьё Макса выше заголовка (стр. 8–222) | раздел 2 «Сырьё Макса — ждёт разбора», дословно |

Осталось открытым (раздел 1): роли игроков · шёпот толпы · мета-нарратив · модификация чужого
эффекта · тип точки интереса · анти-дубли витрины · статический фидбэк и авто-пауза · формулы брони.

Запись в журнал — сессия 2026-07-29 (пять процедурных решений + список найденных долгов).
Гейты зелёные: шапки 180 доков, ссылки 207.

**Развилка на согласование:** разборы положены в приложение `closed-forks.md`, а не внутрь журнала.
Причина — журнал живой и читается каждую сессию, +250 строк таблиц вариантов сделали бы его
нечитаемым. Если Макс хочет строго «всё в журнал» — перенести одним движением.

## Фаза 3 — разбор черновиков (СДЕЛАНО 2026-07-29)

Формат по договорённости: мнение в чат → вердикты Макса → запись. Ресёрч точечный (три темы).

### Вердикты Макса

1. **Лёд — три ступени**, его числа внутрь ступеней (шкала на 20 стаков отклонена).
2. **Спелл-стилер сохраняет d6** — зафиксировано как явное исключение из правила нулевого выходного
   рандома, с ограничением «рандом меняет действие, а не число».
3. **Боссы — карточки-теория + реестр швов.** Поправка: боссов **три**, а не пять — «Дикие звери +
   Друиды» и «Призрачный Корован» это фракции.
4. **Все три правки канона приняты:** Пастырь 2→1, Кузнец → Уникальная, The Tide → водяной монах.

### Что создано

- [[gdd/20-combat/effects/frost]] — холодная линия («Изморозь» + три состояния), числа `proposed`.
- [[gdd/relics/the-torrent]] — наследник The Tide; сам The Tide стал надгробием, дизайн — в легаси.
- `enemies/bosses/` — индекс + три карточки боссов; реестр требуемых швов — roadmap Блок 2.55.
- [[gdd/enemies/species/revenant-caravan]] — заявка на Вид (смерть как ресурс, лёд, скелеты).
- Аркана получила ось (чужой ресурс) в `effects.md`; **немота** введена как CC-примитив.

### Что разнесено (18 позиций)

Карта — в [[gdd/00-meta/open]] §2. Девять новых китов лежат в инкубаторе реликвий с оценкой цены;
поштучных вердиктов по ним ещё нет — это следующий заход.

### Ресёрч (три темы, точечно)

- **Стакающийся холод:** Warframe держит холод на 10 стаках с порогом Frozen; статья Doucet о
  стакинге советует «потенция + длительность» и перекрытие сильнейшим. Аргумент в пользу ступеней.
- **Спелл-стилер:** у Rubick самые сложные взаимодействия во всей игре, каждое требует документации;
  главная цена — поддержка, а не сила. Отсюда требование флага «крадётся / нет» на способностях.
- **Opt-in модификаторы:** заметка Макса совпала с Pact of Punishment (Hades) — модификаторы с
  ранками, растящие награду; признанная практика против фиксированных пресетов.

### Открытые хвосты (для следующего захода)

1. Поштучные вердикты по девяти китам (шесть рекомендованы, два отложены, один блокирован вопросом).
2. Приёмка чисел холодной линии + риск сложения периодического обездвиживания от двух источников.
3. Вопрос по Демону снов: **пробуждается ли цель от урона.**
4. Мана-дрейн Арканы против безресурсных китов (три выхода, рекомендация — «бонус, не ядро»).
5. Имя The Torrent — подтвердить или заменить.
6. Термин «Командир» vs «Капитан» — рекомендация: второй термин не вводить.
7. Три возражения скрайба без вердикта: таймер расстановки 30 сек · доплата за выживших · «flat-часть
   брони» при процентной формуле.
