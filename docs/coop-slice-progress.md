# Кооп: ресёрч и решения перед ТЗ

**Заход открыт 2026-07-31.** Вопрос Макса: «мультиплеер всегда дорожает разработку — так ли это?
можно ли покрыть кооп тестами, как у нас покрыто ядро? насколько плохая идея начать базу параллельно
(все видят бой + все перетаскивают юнитов и жмут в бой)?»

Файл — **журнал захода**, не канон. Когда из него вырастет ТЗ, оно уедет в
`docs/wiki/tech/40-planning/`, а принятые решения — записями в `docs/wiki/tech/00-meta/journal/`.
Здесь остаётся ход мысли и то, что в ТЗ не влезет.

---

## 1. Что в проекте уже есть (инвентарь на 31.07.2026)

Проверено по живому коду, не по докам.

| Есть | Где | Состояние |
|---|---|---|
| Транспорт Steam | `Assets/_Project/Scripts/Net/FacepunchTransportBootstrap.cs` | инициализация Steam на AppId 480 (Spacewar), больше ничего |
| Релей команд | `Assets/_Project/Scripts/Net/NetworkCommandRelay.cs` | только `Pause`/`Resume`, обвязка в стиле lockstep-широковещания (сам файл это признаёт в `<remarks>`) |
| Проба чек-сумм | `Assets/_Project/Scripts/Net/_Parked/SimSyncProbe.cs` | запаркована, зовёт `CombatSimulation.ComputeChecksum()` |
| Гейт готовности | `Assets/_Project/Scripts/Game/Flow/RunFlowSeams.cs` | шов `IReadyGate` + соло-заглушка; `IPlayerIntentSource.IsLocalAuthority` |
| Боевая лента | `Assets/_Project/Scripts/Combat/Tape/` | **готова**: события за весь бой, снимки кольцевым окном, показ с лагом 10 с |
| Детерминизм | `Core/Random/IRngService`, `Combat/CombatSimulation.ComputeChecksum` | есть; сид боя ставит `BattleBootstrap.ReseedForBattle` |
| Глаголы записи в забег | `Guild/RunStateService.SetSlotPosition` / `SetSlotRelic` | есть — это будущие команды |
| Пакеты | `Packages/manifest.json` | NGO 2.11.2, MPPM 1.3.2, multiplayer.center 1.0.1. **Multiplayer Tools НЕТ** |
| Тесты | `Assets/_Project/Tests` | 925 тестовых атрибутов в 135 файлах |

**Главный капитал захода:** гарант Макса от 28.07 — «игрок на бой не влияет вообще». Из него уже
выросла лента показа, и он же убирает из задачи самый дорогой класс сетевой работы — синхронизацию
ввода в живую симуляцию. У нас этот класс **пустой**, а не дешёвый.

---

## 2. Внешний ресёрч (31.07.2026)

### Сколько на самом деле стоит мультиплеер

- **«Онлайн-мультиплеер примерно удваивает время программирования, особенно в первый раз»** — Йост
  ван Донген (Ronimo, *Awesomenauts*). Рядом в той же статье: *SpeedRunners* — двое человек и
  «несколько лет» на онлайн; *Chompy Chomp Chomp* — +3 месяца к 6-месячному циклу.
  Источник: <https://www.gamedeveloper.com/design/the-ups-and-downs-of-doing-online-multiplayer-as-an-indie>
- **Дорога НЕ синхронизация геймплея.** Ван Донген называет расходные статьи прямо: матчмейкинг,
  инвайты, обработка сетевых ошибок и краевые случаи («принял инвайт во время катсцены → принял
  второй на загрузке → сетевая ошибка на второй загрузке»).
- **Вторая статья расходов — отладка.** Разработчик *Gun Monkeys*: поднять два инстанса и
  воспроизвести баг в одиночку на двух копиях «занимает вечность».
- **Совет оттуда же, взят в работу:** не гнаться за идеалом в первой версии — цель первой итерации,
  чтобы игроки поверили, что находятся в одном мире, а не пиксельная точность синхрона.

**Что это значит для нас:** множитель ×2 — к **программированию**, а не к проекту. Оставшаяся работа
у нас в основном контент, арт, баланс, UI, нарратив — множитель к общему объёму сильно меньше двух.
Но он не нулевой там, где я сперва сказала «почти бесплатно»: лобби, инвайты, реконнект, «хост ушёл» —
это и есть основная цена, и у нас её **нет вообще**.

### Тесты: у метода есть имя

То, что мы хотим сделать, в мире распределённых систем называется **Deterministic Simulation Testing
(DST)**. Так тестируют FoundationDB, TigerBeetle, WarpStream; на этом построена Antithesis. Суть:
систему гоняют **в одном потоке на симуляторе**, детерминизируют ВСЕ источники недетерминизма —
часы, порядок потоков, случайность, доставку, — и добавляют инъекцию отказов. Выигрыш: баг
**воспроизводится по сиду**, и часы реального поведения проматываются за секунды.
Источники: <https://antithesis.com/docs/resources/deterministic_simulation_testing/> ·
<https://journal.resonatehq.io/p/deterministic-simulation-testing>

**У нас половина метода уже стоит** (детерминированное ядро, `IRngService.Reseed`, `ComputeChecksum`,
`SimSyncProbe`). Дотянуть надо на сетевой слой: подменяемыми обязаны быть **часы, доставка пакетов и
порядок применения команд**.

### Инструменты, проверено по докам

- **`NetcodeIntegrationTest`** в `Unity.Netcode.TestHelpers.Runtime` (NGO): несколько `NetworkManager`
  в ОДНОМ процессе (`m_ClientNetworkManagers[]`), плюс `MockTimeProvider` (управляемое время),
  `NetcodeLogAssert`, `TimeoutHelper`, `HostOrServer` как параметр фикстуры. Это лекарство ровно от
  боли «воспроизводить баг на двух копиях вечность».
  <https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.10/api/Unity.Netcode.TestHelpers.Runtime.NetcodeIntegrationTest.html>
- **MPPM** (у нас 1.3.2): до 4 игроков = главный редактор + 3 виртуальных, одни ассеты на диске.
  Ограничения: жёсткий лимит 4, каждый виртуальный игрок ест полную память рантайма, **в редакторных
  инстансах нельзя менять объекты** — правки только в главном редакторе.
  <https://docs.unity3d.com/Packages/com.unity.multiplayer.playmode@1.6/manual/virtual-players/virtual-players-enable.html>
- **Network Simulator** (задержка, джиттер, потеря, лаг-спайки) живёт в **Multiplayer Tools и работает
  с UTP**. См. ловушку в §6.
  <https://docs.unity3d.com/Packages/com.unity.multiplayer.tools@2.2/manual/network-simulator.html>

### Про раздачу боя

- **Паттерн жанра подтверждён:** инди-автобатлер *Node Wars*, девлог «Pre-Calculated Battles + Time
  Controls» — бой считается целиком заранее и проигрывается «как видео», один сид → один исход,
  отсюда бесплатно реплеи, пауза и скраб; в сетевой игре сервер считает бой и отдаёт клиентам данные
  для воспроизведения. (Страницу itch отдал под 403 — суть из поисковой выдачи, цитат нет.)
- **Против «вход+сид, считаем локально»:** Глен Фидлер, «Deterministic Lockstep» — детерминизм
  `float` между компиляторами, ОС и архитектурами почти не гарантируется, **даже debug против release
  расходится** из-за оптимизаций. У нас один билд и одна платформа, шанс «бит-в-бит» неплохой, но
  гарантии нет: JIT вправе выбирать инструкции по CPU игрока.
  <https://gafferongames.com/post/deterministic_lockstep/>
- **Приятная находка:** приём «буферизуем немного, чтобы дальше кадры шли ровно» у Фидлера называется
  **playout delay buffer** (аналогия с Netflix). Наш 10-секундный лаг показа — он же, только
  промышленного размера. То есть у нас уже стоит промышленный узел с именем.

---

## 3. Четыре класса синхронности и наша цена

Классы — из дизайна (`docs/wiki/gdd/50-modes-ux/coop/sync-model.md`), цена — оценка техов.

| Класс | Что там | Цена у нас | Почему |
|---|---|---|---|
| 1. Кино (бой) | бой | **почти нулевая** | лента готова, ввода в бою нет ни у кого |
| 2. Транзакции | казна, драфт, расстановка, найм, голосование | **основная цена** | сидит на экранах, которые сейчас переписываются |
| 3. Присутствие | курсоры, драг, пинги | дешёвая | потери допустимы, механика их не читает |
| 4. Локальный шум | камера, зум, тултипы, звук | ноль | это не сеть |

Плюс класс, которого в дизайнерском делении нет, а в смете он есть: **сессия** — лобби, инвайты,
дисконнект, реконнект, «хост ушёл». По внешним свидетельствам (§2) именно он и есть настоящая цена
мультиплеера.

---

## 4. Вердикт по открытому тех-вопросу (tech-debt §4.1)

> **ПРИНЯТО 01.08.2026** — с существенной поправкой к оценке цены, см. §11. Запись в журнале:
> `2026-08-01-the-tape-ships-as-events-plus-packed-snapshots`. Прикидка трафика ниже («сотня-две
> килобайт», «сериализуются тривиально») **занижена на порядок** — она считала события и не считала
> снимки. Оставлена как есть, чтобы ошибка была видна вместе с тем, чем её лечили.

Вопрос «как раздавать боевую ленту по сети» поднял дизайн, вердикт был за техами. **Моя
рекомендация (ждёт «ок» Макса):**

**Раздавать поток событий ленты чанками — append-only, с тиковым штампом. «Вход + сид» держать как
будущую оптимизацию за чек-суммой.**

Обоснование:
- события — плоские `readonly struct` из примитивов (`TapeEvent`, `UnitSnapshot`), сериализуются
  тривиально; прикидка — **сотня-две килобайт на бой**, один поток за секунду до показа;
- нулевой риск «разные картинки»: исход считает хост, клиент ничего не пересчитывает;
- совпадает с требованием пари: «пока открыты ставки, лента не уходит игрокам даже фрагментом» —
  а раздача целиком как раз требует просчитать бой до конца ПЕРЕД показом;
- `sync-model` п.5 разрешает задержку перед боем; headless-прогон 1200 тиков стоит доли секунды;
- аппенд решает «внешнее влияние на бой»: хвост пересчитан → догналась дельта.

**Шаг развития, а не ставка:** расконсервировать `SimSyncProbe` и в dev-режиме собирать статистику
чек-сумм с реальных машин. Держатся бит-в-бит месяцами → можно перейти на «вход+сид» и сделать
трафик почти нулевым. Не держатся — мы ничего не потеряли.

**Следствие, которое надо проговорить:** это переосмысляет решение 19.06. «Host-authoritative»
остаётся верным про авторитет, но «реплицировать состояние юнитов через NGO каждый тик» становится
не нужно — гарант 28.07 удешевил задачу задним числом. Это отдельная запись в журнал, если Макс
согласится.

---

## 5. Паттерны, которые кладём в основу

1. **Единственная запись в общее состояние — команда.** `RunState` мутируется только через командную
   шину: локальный клик → интент → хост принял → команда с номером → применилась у всех. Половина
   готова: `RunStateService.SetSlotPosition`/`SetSlotRelic` — уже глаголы записи, их надо завернуть, а
   не изобретать. Команды **идемпотентны по номеру** — иначе реконнект даст дубли.
2. **Транспорт за интерфейсом, с loopback-реализацией.** Всё, что выше интерфейса, тестируется в
   EditMode без NGO вообще. Без этого шва тесты на кооп физически не написать.
3. **Бой — поток событий по показу.** Часы показа локальные (уже так: `BattleTapePlayback`),
   отстающий догоняет сам. Ничего не менять — уже правильно.
4. **Отказ предсказывается, а не показывается.** Расстановка — оптимистично локально; казна — мягкая
   заявка «кто-то уже смотрит эту позицию» ДО клика.
5. **Снимок сессии = сейв-конверт.** Late-join и реконнект не требуют своего формата: durable-состояние
   забега уже плоский DTO по строковым id. Реконнект — это `run.json` по сети.
6. **Присутствие — ненадёжный канал и ноль механики.** Как только от жеста что-то зависит, он обязан
   переехать в класс 2 и стать транзакцией.

---

## 6. Находки и ловушки

- **ЛОВУШКА: Network Simulator не подойдёт к нашему транспорту.** Он живёт в **Multiplayer Tools и
  работает с UTP**, а у нас **Facepunch/Steam relay**, и Multiplayer Tools в манифесте нет. Вывод:
  держать **два транспорта** — UTP для dev и тестов (там работают симулятор сети и chaos-сценарии),
  Facepunch для релиза, — плюс свой chaos-слой за интерфейсом для EditMode. Дёшево, если шов заложен
  сразу; мучительно, если Steam-транспорт прирастёт к коду.
- **Сид боя в боевом скоупе — нулевая константа.** `CombatLifetimeScope.cs:119` регистрирует
  `new XorShiftRng(0UL)`; спасает `BattleBootstrap.ReseedForBattle` перед боем. Работает, но в коопе
  это будущий источник «а почему у нас разные бои», если какой-нибудь путь запуска забудет пересев.
  Дешёвая страховка — тест «любой запуск боя проходит через пересев».
- **`NetworkCommandRelay` наполовину легаси.** Обвязка широковещания «все применяют на одном тике» —
  из lockstep-эпохи; при гаранте 28.07 её смысл сузился до Pause/Resume. Переписывать при сборке
  вертикали, не раньше.
- **AppId 480 (Spacewar)** — тестовый. Перед живым коопом нужен свой.
- **Десинка правил у нас быть не может** (возражение Макса 31.07, принято): хост считает всё, клиенты
  только просят. Остаются ТРИ других класса, и путать их с десинком вредно:
  - **доставка** — клиент получил не все события или не в том порядке; расходится картинка, не
    правила. Лечится нумерацией чанков и надёжным упорядоченным каналом;
  - **откат обещания** — оптимистично показали, хост отказал (класс 2);
  - **реконнект** — дубли и потери команд на стыке; лечится идемпотентностью по номеру.
- **Аргумент про десинки в *Across the Obelisk* снят** (31.07): механизм их расхождения не проверен,
  и он бьёт только по варианту «каждый считает локально», который мы не выбираем.

---

## 7. План вертикали (порядок важен)

Начинать **не с боя** — он самый дешёвый и самый готовый, его логично делать третьим.

1. **Транспорт за интерфейсом + loopback + UTP-профиль для dev.** ~полдня, снимает главную боль
   отладки на годы вперёд.
2. **Сессия: лобби, инвайт, дисконнект, реконнект.** Настоящая цена мультиплеера. Скелет есть:
   `IReadyGate` заглушкой в `RunFlowSeams.cs`, снимок сессии = сейв-DTO забега.
3. **Бой-как-кино:** раздача ленты чанками (см. §4).
4. **Присутствие:** курсоры. Дешёвое и сразу продаёт «мы вместе».
5. **Транзакции над экранами подготовки — ОТЛОЖИТЬ**, оставив ОДИН шов: запись в `RunState` только
   через командную шину.

**Почему пункт 5 отложен.** ИСПРАВЛЕНО 31.07: я обосновывала это тем, что «под слоем движется пол —
реворк UI в плане, лоадаут в редизайне». **Это неверно.** Реворк UI-архитектуры **завершён 19.07**
(Ф0-Ф7, коммит `59965f7b6`), а его журнал прогресса Макс удалил 30.07 (`026f27aed`) как отработанный,
вынув из него ценное. Лоадаут-хаб **снесён** тем же коммитом — теперь `LoadoutScreen` +
`LoadoutInventoryScreen`.

Настоящая причина отложить пункт 5 — **не риск переписывания, а объём и приоритет**: транзакции над
подготовкой это самый большой кусок работы, и для первой вертикали («смотрим один бой вместе, видим
курсоры») они не нужны. Шов командной шины закрывает вопрос ретрофита.

Что из UI действительно не сделано (сверено по коду 31.07): **Трек Х** — UI-хоткеи режимов (в
`InputService` карты Camera/Combat/Deployment/Pointer/UI, хоткеев M/I/B нет); **Трек К** — своя
DevConsole вместо QFSW (QFSW жив в `Plugins/`, дев-команды на нём); **Трек В** — флипбуки/видео (нет
ни `FlipbookView`, ни `VideoView`); **ConfirmDialog** на «В меню» (отложен ещё в реворке). Сделаны:
**Трек Т** (`Scripts/UI/Tooltips/` + `TooltipCard`) и **Трек Д** (`UI/Theme/tokens.*`, `components.uss`
— живёт и правится). Из этого коопа касается только Трек К: дев-команды станут хост-авторитативными
бесплатно, если пойдут через ту же шину.

**Состав тестов вертикали:** прогон одного лога команд через две модели → одинаковый `RunState`;
chaos-транспорт с задержкой/потерей/переупорядочиванием из сида; конкурентная транзакция (двое тратят
одни деньги в один тик — кто получил отказ и был ли он предсказан); реконнект (снимок → восстановление
→ сверка); идемпотентность команды по номеру.

---

## 8. Открытые вопросы к ТЗ — ВСЕ ЗАКРЫТЫ 01.08.2026

ТЗ написано: `docs/wiki/tech/40-planning/coop-vertical.md`. Ответы:

1. **Формат чанка ленты** — своим кодеком, §5 ТЗ: квантованная позиция + dirty-дельта на редкие поля,
   определения строковыми id, размер чанка десятками КБ, потеря → повтор по номеру, разрыв больше окна
   → опорный снимок. Число тиков в чанке подбирается замером, а не назначено.
2. **«Хост ушёл»** — **конец сессии** (Макс, 01.08). Миграция авторитета не пишется вовсе.
3. **UTP-профиль** — **постоянная dev-настройка** (моей властью). В prefs не выносится: выбор делает
   сборка, игроку выбирать нечего. Переключать транспорт руками ради воспроизведения бага = не
   воспроизводить его вовсе.
4. **Идемпотентность** — пара **`(PlayerId, Sequence)`** (Макс, 01.08). Клиент нумерует сам, иначе
   оптимистичный локальный отклик требует второго механизма рядом.
5. **Лобби-экран** — в вертикаль **не входит**: хватает Steam-оверлея и
   `OnGameLobbyJoinRequested`. Свой экран заводится, когда появится причина сложнее «пустить друга».
6. **`SimSyncProbe`** — **по dev-флагу** (моей властью). Он собирает данные для будущей оптимизации, а
   не участвует в раздаче боя.

---

## 10. Паттерны под наши кооп-идеи (ресёрч, раунд 2 — 31.07.2026)

Заход по просьбе Макса: не «как вообще делают сеть», а **что взять под конкретные наши идеи** —
курсоры, общая казна, кубик и споры, драг юнитов, лобби, реконнект.

### A. Присутствие: модель Figma, один в один наша

Figma broadcast'ит presence — курсор, выделение, видимую область — **по тому же каналу, что и правки
документа, но никогда не пишет её в журнал и не сохраняет**: presence эфемерна by design. Данные
присутствия **склеиваются и отправляются на ~30 FPS** (coalesced), а конфликт правок решается
**last-writer-wins на свойство объекта**: две одновременные правки независимы, если не трогают одно
свойство одного объекта; трогают — берём последнюю.
<https://www.figma.com/blog/multiplayer-editing-in-figma/>

Что берём:
- курсор + наведение + «что держит» — **один склеенный пакет на игрока, ненадёжным каналом, 60 Гц**, с
  интерполяцией на приёме. Не `NetworkVariable`, не RPC на каждый кадр. Про выбор частоты — §A1;
- **эфемерность как правило кода:** присутствие не сохраняется, не попадает в лог команд и не влияет
  на `RunState`. Это ровно наш HARD «ни одна механика не читает присутствие» — теперь с внешним
  подтверждением от продукта, где это работает на миллионах сессий;
- **чужое выделение видно** — это и есть «мягкая заявка на объект» из нашего `presence.md`, только
  Figma доказала, что её достаточно и локов не нужно;
- NGO-гигиена: **не шлём по `Update`, шлём по тику с dirty-check** (иначе спам канала), и
  `RpcTargetUse.Temp` против аллокаций `BaseRpcTarget`.
  <https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.5/manual/learn/ticks-and-update-rates.html>

### A1. Частота присутствия: 60 Гц, и почему не 30 и не 240

Вопрос Макса 31.07: «сеть у нас почти не нагружена, у людей мониторы 165 Гц — почему не гнать чаще?»
Он прав по выводу (30 занижено), но механизм другой, и именно механизм задаёт цифру.

**Частота отправки НЕ покупает плавность — плавность даёт интерполяция на приёме.** У Фидлера
снапшоты летят 10 раз в секунду, а картинка гладкая, потому что между ними интерполируется (линейная
даёт лёгкое дрожание на изломах, Hermite по скорости — нет). Частота покупает другое: **размер буфера
интерполяции**, а через него — задержку. Практическое правило оттуда же — буфер ≈ **3 интервала
отправки** при 2-5% потерь: 10 Гц → ~300 мс, 30 Гц → ~150 мс, **60 Гц → ~85 мс**.
<https://gafferongames.com/post/snapshot_interpolation/>

**РЕШЕНИЕ МАКСА 31.07: 128 Гц, dirty-check, короткий буфер.** Отправка только по факту изменения
(курсор стоит — ноль пакетов), потолок 128 Гц, буфер 1-2 интервала (~8-16 мс) вместо трёх: presence
эфемерна, потерянный пакет экстраполируется по последней скорости и никто не замечает. Возить скорость
курсора ради Hermite. Все чужие курсоры — одним склеенным пакетом. Цена: ~20 байт на курсор, единицы
КБ/с. Настройку в prefs НЕ заводим: цена постоянная, выбирать нечего.

Обоснование выбора **128, а не 60**: у Макса кооп-взаимодействия курсоров мелкие и быстрые (перехват из
рук, кубик, печати), сеть у нас почти не нагружена, и разница ~16 мс на слое буфера покупается даром.
Моя прежняя цифра 60 занижена — «выигрыш тонет в RTT» верно для 120→240, но не для 60→120.

**Почему не 240:**
- **RTT доминирует.** По Steam relay дорога стоит 30-80 мс независимо от числа пакетов. С 60 до 240
  буфер падает с ~85 до ~25 мс — выигрыш тонет в том, что мы не контролируем.
- **Узкое звено решает.** Доки NGO прямо: если update rate клиента ниже tick rate сервера, клиент
  **не увидит выгоды** от высокого тика. Рваные 240 = голодающий буфер и дрожание; стабильные 60
  честнее.
  <https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.5/manual/learn/ticks-and-update-rates.html>
- **Пакеты стоят не только байтами:** сериализация, очереди и аллокация массива на каждую отправку
  named message (issue #2779) — тысячи аллокаций в секунду ради неразличимого сигнала.
- Со стороны источника: рука на мыши — низкочастотный сигнал; на 60 Гц ошибка при резком рывке — единицы
  пикселей, выше передаётся шум сенсора.

**Про бой вопрос не имеет предмета: во время боя сеть не участвует в плавности — там НОЛЬ пакетов.**
Лента раздана чанками до показа, дальше каждый проигрывает локально своими часами. Плавность боя уже
обеспечена локально (`PreviousPosition` в снимке, доля кадра от реального времени, фазировка удара
числами) — на 165-герцевом мониторе бой уже рисуется 165 кадрами. Тик 30 Гц — частота МОДЕЛИ, и он
неприкосновенен по несетевым причинам: на него привязаны баланс, чек-суммы и зеркальные тесты. 30 Гц
ограничивает только **гранулярность момента события** (33 мс) — это вопрос подачи, лечится таймингом
анимации, а не тиком.

### A2. Быстрые взаимодействия курсоров — это ТРАНЗАКЦИИ, и частота им не поможет

Заявка Макса 31.07: «взаимодействия курсоров ППЦ быстрые и мелкие, поэтому и хочется меньшую
задержку». Поправка, важная для ТЗ: перехват вещи из рук, пнуть кубик, печать, чокнуться — **механика**,
значит класс 2. Их задержка — **полный RTT до хоста и обратно** (60-160 мс), а не буфер интерполяции.
128 Гц делают красивым ВИД чужой руки, но самого перехвата не ускоряют.

Лечится двумя вещами:

1. **Оптимистичный локальный отклик:** своё действие играется сразу, хост подтверждает. Дизайн к этому
   уже готов — перехват задуман соревнованием, и «не успел» законный исход («обратимое хулиганство»).
2. **Порядок по КЛИЕНТСКОМУ времени, а не по времени прибытия** — иначе у хоста RTT нулевой и он
   **всегда** выигрывает гонку за кубик и за вещь в воздухе. Развилка сознательная: принять преимущество
   хоста ИЛИ штамповать интенты клиентским временем по синхронизированным часам. Второе дёшево — общее
   время всё равно нужно для «3…2…1» при возобновлении после дисконнекта.

### A3. Sub-tick подача событий: что у нас есть и что даст (проверено по коду 31.07)

Вопрос Макса: «хочу, чтобы визуал и сим норм работали вместе — анимации, их ивенты». Проверила код,
картина такая.

**Уже сделано:**
- **Поза течёт непрерывно, не в 30 Гц.** `UnitAnimationSelector.ScrubProgress` скрабит клип по счётчику
  тиков **плюс `frameAlpha`** — доля внутри показываемого тика, тем же множителем, что и позиция. Без
  неё было бы «6 поз на замах в 6 тиков», и это читалось бы как «визуал в 30 Гц».
- **Урон не привязан к анимации — наоборот.** `Animator.fireEvents = false`, маркеры не колбэчат: сим
  берёт кадр контакта из клипа (`ClipMarkers.HitFunction` → `AttackHitFrame`/`AttackFrameCount`) и
  считает `WindupTicks` целочисленно с клампами. Владелец момента — тик, анимация подстраивается.
- Поток авто-атаки крутится циклом от маркера (`ChannelClipTime`), чтобы кадр контакта приходился на
  КАЖДЫЙ удар канала.

**Разрыв ровно один:** `BattleTapeDispatcher.PumpTo(viewTick)` отдаёт события на **границе тика**, без
`frameAlpha`. Отсюда перекос точности:

| Слой | Точность момента |
|---|---|
| поза, позиция | кадр (~6 мс на 165 Гц) |
| вспышка, цифра урона, звук удара, hitstop, шейк, slowmo | **граница тика (33 мс)** |

Меч касается внутри тика, вспышка щёлкает на границе: разброс 0-33 мс, в среднем ~16. Ощущается как
«вялый удар» / «звук не в кадр» при идеально посчитанном уроне.

**Что даст sub-tick подача:** одно поле в `TapeEvent` — доля тика [0..1) — и сравнение в `PumpTo` по паре
(тик, доля). Вспышка, звук, цифра и hitstop встают в тот кадр, где меч коснулся; точность с 33 мс до кадра.

Почему это дёшево и безопасно:
- **сим не меняется вовсе** — доля считается при ЗАПИСИ события из данных вида (положение маркера ×
  длина свинга), момент урона в модели остаётся тиком N. Баланс, чек-суммы, зеркальные тесты, бенчи не
  двигаются;
- **подписчики не меняются** — правка в одном месте, `PumpTo`;
- это приём Valve (CS2 Subtick), но штамп живёт в **подаче**, а не в правилах — как и требует HARD
  «показ подаёт, но не переписывает».

**Чего это НЕ лечит:** если урон должен случаться между тиками по-настоящему (снаряд пролетает мимо цели
за тик) — это правка модели, и ответ там другой (проверка пути, не частота).

**Статус:** предложение, ждёт вердикта Макса. Заход самостоятельный, к коопу не привязан — но в коопе
ценен тем, что доля тика едет внутри события ленты и раздаётся бесплатно вместе с ней.

### B. Транзакции: уроки из инвентарных дупов

Классика багов общего инвентаря даёт готовый набор правил, и они прямо накладываются на нашу общую
казну:

- **Один авторитетный путь на все изменения.** Не «несколько мест добавляют и снимают», а один
  валидирующий вход — иначе гонка неизбежна.
- **Идемпотентность по `change_id`:** каждая транзакция несёт уникальный id, повтор с тем же id
  **отбрасывается**. Это лекарство от «двойного золота» и от дублей команд при реконнекте.
- **Атомарность:** снять с источника и добавить в приёмник — одной операцией. Add-before-remove,
  прерванный на середине, оставляет предмет в двух местах.
- **Debounce / метка «израсходовано»** против двойного срабатывания одного события.
- Худший из известных сценариев дупа — **две копии состояния, загруженные асинхронно**, каждая
  сохранилась. У нас аналог ровно один: второе место записи в `RunState`.
  <https://bugnet.io/blog/how-to-fix-inventory-item-duplication-bug> ·
  <https://gamecoderstudios.com/how-we-built-a-worldwide-inventory-sync-for-a-crafting-heavy-mmo/>

### C. Event sourcing: у нас он уже есть — в бою

**Append-only лог как источник правды, состояние — проекция лога.** Даёт бесплатно: реплей, аудит
(«кто передвинул юнита»), тест «один лог команд → одинаковый `RunState`», реконнект (снимок + хвост
лога). Расширяемость идёт оттуда же: **обработчик команды порождает события, реакции подписаны на
события** — добавить новую реакцию не значит тронуть команду.
<https://learn.microsoft.com/en-us/azure/architecture/patterns/event-sourcing> ·
<https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/event-sourcing.html>

**Красивая симметрия, которую стоит осознать:** `BattleTape` — это уже event sourcing для боя
(события за весь бой, состояние — проекция на момент показа). Значит кооп не приносит новой парадигмы:
**тот же приём применяется к забегу**, только лог не боевых событий, а команд игроков. Одна голова на
две ленты — это и есть «расширяемость» в нашем случае.

### D. Как физически летит лента (NGO-механика)

- **Custom named messages** через `NetworkManager.CustomMessagingManager`:
  `RegisterNamedMessageHandler` / `SendNamedMessage` / `SendNamedMessageToAll`, сериализация —
  `FastBufferWriter` / `FastBufferReader`. Это сообщения, **не привязанные ни к какому GameObject** —
  ровно то, что нужно чанкам ленты (у ленты нет и не должно быть `NetworkObject`).
  <https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.12/manual/advanced-topics/message-system/custom-messages.html>
- **Размер:** надёжные payload'ы у UnityTransport **вправе превышать настроенный Max Payload Size**, а
  ненадёжные — нет. Значит чанки ленты идут `ReliableFragmentedSequenced`, присутствие — ненадёжным и
  мелким.
- **Готча производительности:** named messages в NGO аллоцируют `byte[]` на каждую отправку (issue
  #2779) — писать сразу в переиспользуемый `FastBufferWriter`, а не собирать массивы.
  <https://github.com/Unity-Technologies/com.unity.netcode.gameobjects/issues/2779>
- NGO **батчит последовательные RPC** к одному клиенту — отдельного батчера писать не надо.

### E. Сессия: лобби Steam через Facepunch — почти готовый флоу

Своё меню инвайтов писать не нужно, всё есть в Facepunch: `Lobby` (создать/войти, `RoomEnter.Success`),
`Lobby.InviteFriend`, `SteamFriends.OpenGameInviteOverlay` (оверлей приглашения), и главное —
**`SteamFriends.OnGameLobbyJoinRequested`**: игрок жмёт «Join» в списке друзей, игра получает событие
и подключается к лобби. Плюс rich presence.
<https://wiki.facepunch.com/steamworks/Data.Lobby> ·
<https://wiki.facepunch.com/steamworks/SteamFriends.OnGameLobbyJoinRequested>

Стыковка с NGO — **Connection Approval**: NGO делает свой handshake поверх транспортного и позволяет
решать по каждому подключению, пускать ли, с таймаутом на ответ.
<https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.7/manual/basics/connection-approval.html>

### F. HARD-кандидат: handshake версии контента — сразу, не «потом»

Мод *Spire with Friends* (кооп и версус для Slay the Spire) даёт прямое свидетельство: **десинки в
версусе вызваны РАЗНЫМИ контент-модами у игроков, и при одинаковых сетапах всё работает**.
<https://steamcommunity.com/sharedfiles/filedetails/?id=2395241968>

У нас data-driven контент на строковых id, публичный репозиторий и живой поток правок в SO. Значит
**в Connection Approval обязан ехать отпечаток контента + версия игры**, а отказ — с понятным текстом
(«у вас другая версия контента»), а не молчаливым разъездом. NGO сверяет свой `NetworkConfig` hash, но
**про наш контент он не знает ничего** — это наша работа.

### G. Что из этого меняет план §7

- Шаг 1 (транспорт за интерфейсом) дополняется: **handshake версии/контента — часть шага 1**, не
  отдельная задача. Дешевле на старте, чем после первого «а почему у нас разные реликвии».
- Шаг 2 (сессия) частично готов у Facepunch — оценка вниз: инвайты и join-по-другу это события, а не
  подсистема.
- Шаг 3 (лента) получает конкретный механизм: named message + `FastBufferWriter` + fragmented reliable.
- Шаг 4 (присутствие) получает готовые числа: 20-30 Гц, coalesce, unreliable, интерполяция.
- Появился шаг 0, которого в плане не было: **лог команд забега как append-only** — он же даёт тесты,
  реплей и реконнект. Без него шаг 5 (транзакции) не спроектировать честно.

---

## 9. Позиция Макса (зафиксировано 31.07.2026)

- **Возразил на десинк и был прав:** «у нас всё считает хост, клиенты лишь отправляют запросы». Мой
  аргумент снят, см. §6.
- **Выбрал вариант A:** сперва тех-ТЗ на кооп-вертикаль и вердикт §4.1 записью в журнал, код пока не
  трогаем. Перед ТЗ — ещё разговор.

**Вердикты 31.07 (все приняты Максом):**

| Что | Вердикт |
|---|---|
| Частота присутствия | **128 Гц + dirty-check + короткий буфер** (§A1) |
| Sub-tick подача событий ленты | **делать** — отдельным заходом, к коопу не привязан (§A3) |
| Порядок конкурирующих интентов | **по клиентскому времени** на синхронных часах — преимущество хоста не принимаем (§A2) |
| Отклик на своё действие | **оптимистичный локальный**, хост подтверждает (§A2) |
| Лог команд забега | **БЕРЁМ обязательно** — append-only, `RunState` меняется только через команды с номерами (§C, шаг 0) |

**Закрыто моей властью (техническое, вердикта не требует):**
- **Курсорный слой реворка UI не ждёт** — он рисуется поверх и не зависит от разметки экранов; иначе
  presence оказалась бы заложницей чужого захода.
- **Отпечаток контента = хеш реестра строковых id + версия сборки.** Хеш ловит расхождение контента,
  версия — расхождение кода; порознь каждый пропускает свой класс поломки.

**Открытых вопросов к Максу больше НЕТ** (31.07). Заход закрыт, следующий шаг — тех-ТЗ на вертикаль в
`docs/wiki/tech/40-planning/` плюс запись вердикта §4.1 в `00-meta/journal/`.
- Кооп для него — важная часть игры, а не опция «если останется время» (позиция прежняя, из
  кооп-кластера ГДД).

---

## 11. Раунд 3 — 01.08.2026: вердикты и поправка к цене

Поводом стал вопрос Макса «теперь мы готовы делать мультиплеер?» после того, как закрылся трек своей
dev-консоли. **ТЗ написано** — `docs/wiki/tech/40-planning/coop-vertical.md`; этот файл дальше живёт
только как история захода.

### Пять решений Макса 01.08

| Что | Вердикт |
|---|---|
| Критерий приёмки вертикали | **двое смотрят один бой + видят курсоры** |
| «Хост ушёл» | **конец сессии**, гости уносят открытия. Миграция авторитета не пишется |
| Номер команды | **пара `(PlayerId, Sequence)`** — счётчик у каждого свой |
| Раздача боя (§4.1) | **события + сжатые снимки чанками** — принято уже с уточнённой ценой |
| Свой Steam AppId | **есть у Макса**, тестовый 480 меняется в фазе Г |

### Поправка, из-за которой вердикт §4.1 пришлось переспросить

Прикидка 31.07 занижала трафик **на порядок**: я посчитала `_events` (пара сотен событий по ~28 байт —
правда копейки) и не посчитала снимки. Показ рисует кадр не из событий, а из `UnitSnapshot` через
`BattleTape.TryGetFrame`; у хоста снимки живут кольцевым **окном**, истории боя в них нет вовсе, а
клиенту нужен снимок каждого тика. Наивно это ~100 байт на юнита на тик — **около 1.4 МБ** за
сорокасекундный бой на двенадцать юнитов.

Спасается форматом (позиция квантованная, `PreviousPosition` не едет вовсе, редкие поля дельтой) до
~100-150 КБ, то есть прежняя цифра оказалась верной как **следствие работы**, а не как свойство ленты.
Разница в выводе: «сериализуется тривиально» превратилось в «день-два на кодек и роундтрип-тест».

**Урок для будущих оценок:** прикидка по структуре данных обязана спросить «а из чего показ рисует
кадр», а не «сколько весит событие». Дешёвая часть механизма заслонила дорогую, потому что у неё
знакомое имя.

### Вторая находка того же чтения: лента держит ССЫЛКИ на ассеты

`_effectDefs` и `_abilityDefs` — списки `EffectData`/`AbilityData`, то есть `UnityEngine.Object`. По
сети едут строковые id с резолвом через реестр на приёме, и **handshake отпечатка контента из
страховки становится обязательным**: неизвестный id роняет показ, а не слегка расходит картинку.

### Третья находка: транспортные пределы у нас не те, что в доках NGO

Проверено по исходнику `FacepunchTransport` (multiplayer-community-contributions) и по константам
Steam:

- транспорт кладёт данные прямо в `Connection.SendMessage(data.Array, data.Offset, data.Count, sendType)`
  — **своей фрагментации нет, размер не проверяется**;
- все reliable-варианты `NetworkDelivery`, включая `ReliableFragmentedSequenced`, сводятся к одному
  `SendType.Reliable`. То есть доки UnityTransport про «reliable payloads вправе превышать Max Payload
  Size» к нашему релизному транспорту **не применимы вовсе**;
- фрагментирует Steam, до **512 КБ на сообщение** (`k_cbMaxSteamNetworkingSocketsMessageSizeSend`),
  сверх лимита — `k_EResultInvalidParam`, **который транспорт не читает**. Слишком большой чанк уезжает
  в тишину.

Источники: <https://partner.steamgames.com/doc/api/steamnetworkingtypes> ·
<https://github.com/Unity-Technologies/multiplayer-community-contributions>

**Вывод в ТЗ:** предел чанка проверяет наш код и отказывает громко — ниже его не проверяет никто.

### Что даёт коопу трек своей консоли

Не то, чего можно было ждать. Боль «поднять два инстанса и воспроизвести баг» лечат loopback-транспорт,
`NetcodeIntegrationTest` и chaos-слой, а не консоль. Консоль — **множитель поверх них**: 35 команд уже
идут через `DevCommandRegistry`, поэтому сценарий «двое, один жмёт, второй смотрит» скриптуется, а
дев-команды, меняющие `RunState`, становятся хост-авторитативными бесплатно — если пойдут через шину
команд из §4.1 ТЗ.

---

## 02.08.2026 — стек сузился до Steam, и что делать дальше

**Вердикт Макса:** «Планирую только Steam» → «РЕЖЬ и давай самый чистый способ напрямую со Steam!».
Высокоуровневого netcode в проекте больше нет. Разбор — журнал
`2026-08-02-the-stack-shrinks-to-steam-and-the-high-level-netcode-goes`; ТЗ `40-planning/coop-vertical`
поправлено сверху, но внутри ещё описывает NGO — читать как историю решения.

**Удалено:** `NgoTransport`, `FacepunchTransportBootstrap`, запаркованный `SimSyncProbe`, объект
`[Net]` из `CoreScene`, пакеты `netcode.gameobjects`, `multiplayer.tools`, `multiplayer.center`,
`multiplayer.playmode`. Границу держит `NoHighLevelNetcodeTests` (код, манифест, сцены).

**Написано и компилируется, но вживую НЕ проверено** (нужен второй Steam-аккаунт с доступом к AppId
3259720 и вторая машина — один Steam-клиент на компьютер):

| Что | Где |
|---|---|
| Транспорт Steam | `Net/Transport/SteamNetTransport.cs` — `CreateRelaySocket` / `ConnectRelay(SteamId)` |
| Рукопожатие | `Net/Session/CoopHandshake.cs` — канал `Handshake`, версия + отпечаток, номер пира от хоста |
| Лобби и приглашения | `Net/Session/SteamLobbyService.cs` — friends-only, оверлей, `OnGameLobbyJoinRequested` |
| Сессия | `Net/Session/CoopSession.cs` — создать одним кликом, пригласить, «хост ушёл = конец» |
| Шов для UI | `Core/Net/ICoopSessionControl.cs` |
| Экран | `UI/Screens/CoopScreen.uxml` + `UI/CoopScreenView.cs`, стенд `Alebardium/UI Preview/Coop` |
| Раздача ленты | `Net/Tape/TapeStreamer.cs`, `TapeIntake.cs`, `BattleTapeBroadcast.cs` |
| Общая пауза | `Net/BattleControlRelay.cs` — пауза как свойство ПОКАЗА |

### Следующий шаг — провод коопа в бой (заказан Максом 02.08)

Всё перечисленное **в боевом скоупе не зарегистрировано** и потому в бою не участвует. Состав работы:

1. **Регистрация в `CombatLifetimeScope`:** `TapeStreamer` (хост), `TapeIntake` (гость),
   `BattleTapeBroadcast` как `ITickable`. Гейт по роли: у гостя стример не нужен и наоборот.
2. **Ветка гостя:** он не тикает симуляцию вовсе — `CombatLoopService` у него не должен запускаться,
   вместо этого `BattleTapePlayback` играет ленту, приходящую чанками. Сейчас цикл безусловный.
3. **Пауза через релей** вместо локального `TimeScaleService`: подписка на `BattleControlRelay.PauseChanged`
   и адаптер в `Game`, потому что `Net` про показ знать не должен.
4. **`RequestMissing`** у гостя надо кому-то звать раз в кадр — тот же тик, что качает транспорт (`NetPump`).
5. **Тест на стыке:** два узла на loopback, хост тикает бой, гость получает ленту и приходит к тому же
   исходу. Это ближайший к «критерию приёмки» тест, который вообще возможен без Steam.

**Готча, не потерять:** `BattleTapeBroadcast.Enabled` — гейт под ставки (пока раздача открыта всегда).
И у гостя нет `CombatSimulation`, поэтому всё, что боевой скоуп собирает вокруг неё, придётся разводить
по роли — это и есть главная работа шага.

### Провод сделан 02.08.2026 — что теперь есть и чего ещё нет

Пять шагов выше закрыты, но не тем составом, каким планировались. **Главное расхождение с планом:**
регистрировать «хосту стример, гостю приёмник» нельзя — боевой скоуп поднимается на буте, в главном
меню, когда роли ещё не существует. Поэтому регистрируются обе половины у всех, а расходятся узлы в
рантайме: роль спрашивается каждый кадр за швом `Core/Net/IBattleAuthority.cs` (`Solo` / `Host` /
`Guest`, выводится из транспорта).

| Шаг | Как сделано |
|---|---|
| Регистрация | `CombatLifetimeScope.RegisterCoop` — читатель, приёмник, стример, раздача, помпа, мост паузы |
| Ветка гостя | `CombatLoopService.GuestFrame` — ни тика, ни снятого кадра, только требуемое отставание |
| Пауза | интент → `BattleControlRelay`, применение → `Game/Services/NetPauseBridge` → `TimeScaleService` |
| Повтор чанков | `Net/Tape/TapeIntakePump` — раз в кадр, на НЕмасштабированном времени (пауза не глушит сеть) |
| Тест на стыке | `CoopBattleWiringTests` — хост тикает живой бой, гость приходит к тому же исходу |

**Сверх плана пришлось сделать состав боя** (`Net/Tape/BattleRosterRelay.cs`, канал `BattleRoster`).
Без него провод неполон: `BattleUnitRegistry` наполняется событием спавна локальной симуляции,
которой у гостя нет, — лента доехала бы, а рисовать было бы нечего.

**Попутно починен дефект конверта:** `NetEnvelope` распознавал канал по рукописному списку рядом с
перечислением. Новый канал в список не попал, и сообщения молча отбрасывались на приёме. Теперь
таблица строится из самого перечисления, инвариант держит `NetEnvelopeTests`.

#### Что осталось открытым (осознанно, не забыто)

1. **Повторный бой у гостя.** `TapeIntake` и `BattleUnitRegistry` у гостя сбрасывать некому:
   `CombatSimulation.OnBattleReset` у него не стреляет — симуляция не тикает. Хост при рестарте
   начинает нумерацию чанков заново, гость примет их за дубли и нового боя не увидит. Лечится
   сигналом «бой сброшен» по каналу `BattleControl` — отдельный маленький заход.
2. **Фаза боя у гостя не синхронизируется.** Показу это сейчас не мешает (отставание у гостя
   постоянное, см. журнал), но UI боевой фазы у него будет молчать.
3. **Вживую по Steam никто не ходил.** Проверено на loopback; настоящий сокет требует второго
   Steam-аккаунта с доступом к AppId 3259720 и второй машины.
