/* Журнал событий: кто что сделал и кто кого убил.

   Разбор 20.08.2026. Журнал и лента — соседи по слою и злейшие двойники: обе строчки, обе про «что
   произошло». Разводятся по адресату. Лента ОБРАЩАЕТСЯ К ИГРОКУ и уходит навсегда; журнал ведёт
   ИСТОРИЮ и ни о чём не просит — его можно перечитать, пролистать и не читать вовсе.

   Практическое следствие: «Гилберт присоединился» и «Крыса убита» идут в журнал, а не в ленту.
   Иначе первый же людный бой закроет лентами пол-экрана. */

import * as w from "./ui-wire.js";
import type { SectionDef } from "../types.js";

/** Строка журнала: время, кто, что, по кому. */
function logLine(
  ctx: CanvasRenderingContext2D,
  y: number,
  width: number,
  height: number,
  dim: boolean,
  label: string
): void {
  w.text(ctx, label, { x: 0.635, y }, width, height,
    { size: 9.5, color: dim ? "#5F5F68" : "#8A8A93" });
}

/** Принято: угол HUD, старые строки тают. */
function drawCorner(ctx: CanvasRenderingContext2D, width: number, height: number): void {
  w.screen(ctx, width, height);
  w.worldBehind(ctx, width, height);

  const area: w.Rect = { x: 0.62, y: 0.62, w: 0.36, h: 0.30 };
  w.box(ctx, area, width, height, { hollow: true, dashed: true });

  logLine(ctx, 0.665, width, height, true, "Гилберт вошёл в лавку");
  logLine(ctx, 0.715, width, height, true, "Крыса-падальщик убита · Мидра");
  logLine(ctx, 0.765, width, height, false, "Мидра · Раскол ↦ Гнильщик");
  logLine(ctx, 0.815, width, height, false, "Гнильщик убит · Мидра");
  logLine(ctx, 0.865, width, height, false, "Хранитель поднял барьер");

  w.callout(ctx, { x: 0.62, y: 0.665 }, { x: 0.44, y: 0.665 },
    "старое тает", width, height, "right");
  w.callout(ctx, { x: 0.62, y: 0.865 }, { x: 0.44, y: 0.865 },
    "новое приходит снизу", width, height, "right");
  w.measure(ctx, area, "36% x 30%", width, height);
}

/** Отклонено: журнала в бою не видно, открывается клавишей. */
function drawWindow(ctx: CanvasRenderingContext2D, width: number, height: number): void {
  w.screen(ctx, width, height);
  w.worldBehind(ctx, width, height);
  w.scrim(ctx, width, height, 0.4);

  const panel: w.Rect = { x: 0.26, y: 0.18, w: 0.48, h: 0.64 };
  w.box(ctx, panel, width, height, {});
  w.text(ctx, "ЖУРНАЛ БОЯ", { x: 0.50, y: 0.235 }, width, height, { align: "center", size: 10 });

  for (let i = 0; i < 8; i++) {
    w.text(ctx, "00:1" + i + "   Мидра · Раскол ↦ Гнильщик", { x: 0.30, y: 0.30 + i * 0.055 },
      width, height, { size: 9.5, color: "#8A8A93" });
  }

  w.callout(ctx, { x: 0.50, y: 0.83 }, { x: 0.50, y: 0.90 },
    "в бою не видно ничего", width, height, "center");
}

/** Отклонено: два облика и тумблер в настройках. */
function drawBoth(ctx: CanvasRenderingContext2D, width: number, height: number): void {
  w.screen(ctx, width, height);
  w.worldBehind(ctx, width, height);

  w.box(ctx, { x: 0.62, y: 0.62, w: 0.36, h: 0.30 }, width, height,
    { hollow: true, dashed: true, label: "угол", size: 10 });
  w.box(ctx, { x: 0.06, y: 0.18, w: 0.34, h: 0.5 }, width, height,
    { label: "окно по клавише", size: 10 });
  w.box(ctx, { x: 0.06, y: 0.75, w: 0.30, h: 0.08 }, width, height,
    { label: "тумблер в настройках", size: 9 });
}

export const section: SectionDef = {
  id: "ui-log",
  title: "Журнал событий",
  eyebrow: "Лаборатория Guildmaster · Интерфейс",
  lede:
    "История боя и сеанса в правом нижнем углу: кто кого ударил, кто кого убил, кто пришёл и куда " +
    "ушёл. Не путать с лентой — та обращается к игроку и требует внимания, журнал только помнит.",
  transport: false,
  blocks: [
    {
      kind: "head",
      id: "what",
      title: "Чем журнал отличается от ленты",
      lede:
        "Оба — строчки текста, оба лезут в слой поверх игры, и без жёсткого правила они сольются. " +
        "Правило про адресата: лента говорит С ИГРОКОМ, журнал ведёт ЛЕТОПИСЬ."
    },
    {
      kind: "table",
      head: ["Признак", "Лента", "Журнал"],
      rows: [
        ["К кому обращено", "к игроку", "ни к кому — это запись"],
        ["Жизнь строки", "секунды, потом навсегда исчезает", "остаётся до конца боя"],
        ["Можно ли перечитать", "нет", "да, прокруткой"],
        ["Место", "справа снизу, НАД журналом", "справа снизу"],
        ["Что попадает", "«нет слота», «связь вернулась»", "«Мидра убила Гнильщика», «Гилберт в лавке»"],
        ["Кликабельность", "клик закрывает", "клик по имени — подсветить юнита (открыто)"]
      ]
    },
    {
      kind: "head",
      id: "layouts",
      title: "Три раскладки",
      lede: "Выбран вариант А (Макс, 20.08.2026): угол HUD, старые строки тают сами."
    },
    {
      kind: "stands",
      items: [
        {
          id: "corner",
          status: "accepted",
          title: "А · Угол HUD, сам гаснет",
          decision: "2026-08-20",
          tag: "приём MOBA",
          note:
            "Пять-шесть последних строк живут в правом нижнем углу, старые тают. Полная история — " +
            "по клавише, поверх этого же угла.",
          facts: [
            ["область", "36% x 30%"],
            ["строк в кадре", "5–6"],
            ["жизнь строки", "~8 с до угасания"],
            ["полная история", "по клавише"]
          ],
          verdict:
            "Событие видно в тот момент, когда оно случилось, и кадр при этом чистый. Цена — " +
            "строки конкурируют за угол с бегущей игрой и требуют читаемости на любом фоне.",
          size: [520, 293],
          draw: drawCorner
        },
        {
          id: "window",
          status: "rejected",
          title: "Б · Только окно по клавише",
          note: "В бою журнала нет вовсе; открывается панелью поверх игры, когда игрок захочет.",
          verdict:
            "Кадр чистейший, но события пролетают мимо глаз: узнать, кто тебя убил, можно только " +
            "задним числом — а спрашивают об этом ровно в момент смерти.",
          size: [480, 270],
          draw: drawWindow
        },
        {
          id: "both",
          status: "rejected",
          title: "В · Оба режима, тумблер",
          note: "Игрок сам выбирает в настройках: угол, окно или ничего.",
          verdict:
            "Два облика вместо одного и тумблер в переписи — при том, что вторым режимом почти " +
            "никто не пользуется. Свободу можно дать позже, когда первый обкатается.",
          size: [480, 270],
          draw: drawBoth
        }
      ]
    },
    {
      kind: "note",
      html:
        "<b>Открыто:</b> состав строки — нужно ли время слева, красить ли имена цветом игрока, " +
        "склеивать ли серию ударов в одну строку со счётчиком · подсвечивать ли юнита при " +
        "наведении на его имя · попадает ли в журнал то, чего игрок не видел (события в чужой части " +
        "арены)."
    }
  ]
};

export default section;
