# Разделение скоупов — ход захода

Временный журнал захода. План и целевая иерархия — `docs/wiki/tech/40-planning/scope-hierarchy.md`,
решения — журналы `2026-08-02-the-levels-of-life-are-named-*` и
`2026-08-02-the-simulation-belongs-to-the-battle-*`. Здесь только ход мысли и состояние работ; когда
заход закроется, ценное осядет в код и журналы, а файл уйдёт.

## Где стоим

| Шаг | Состояние |
|---|---|
| 1а. Источник кадра + держатель тел | код написан, тесты гоняются |
| 1б. Боевой скоуп | не начат |
| 1в. Вырывание легаси | не начат |
| 2. Сессия (Owner / Guest) | не начат |
| 3. Занятие | не начат |
| 4. Узел | не начат |

## Разведка перед резом (02.08.2026)

Проверено чтением, не предположено:

- **`CombatPresenter.Update` рисует только из кадра ленты** — состав видов, снаряды, интерполяция,
  события. Живая симуляция нужна ему для dev-оверлеев (`CombatStatusOverlay`) и подписок. Значит
  поставщика кадра можно подменить.
- **`SimEnvironment` (балансный стенд) уже собирает симуляцию без мира, сети и карты** — критерий
  приёмки 1б выполним, рез проходит по существующей линии.
- **Генераторов случайности два:** забеговый в `RootLifetimeScope` (карта, награды, магазин) и
  боевой в `CombatLifetimeScope`. Боевой держит `CombatSimulation` ссылкой из конструктора и раздаёт
  его системам, `EffectContext` и VFX попаданий — поэтому он обязан родиться вместе с боем.
- **`CombatSimulation` упоминается в 44 файлах**, но внешних потребителей мало: Game (флоу,
  расстановка, ввод, диагностика), Presentation (презентер, оверлеи, камера), Net (раздача, состав),
  DevTools, Balance. Остальное — внутренности `Combat`, которые едут вместе с ней.

## Шаг 1а — что сделано

Шов «кто поставляет кадр показу» и мировой держатель тел:

| Файл | Роль |
|---|---|
| `Combat/Tape/IStageFrameSource.cs` | контракт поставщика кадра: `Advance`, `Alpha`, `TryGetFrame` |
| `Combat/Tape/WorldBodyStage.cs` | тела вне боя: список снимков, ни тика, ни времени |
| `Combat/Tape/StageFrameRouter.cs` | единственный владелец факта «что сейчас на арене» |
| `Game/Flow/BattleStageBinder.cs` | бой сам подключает свою ленту и сам отключает |

`BattleTapePlayback` реализует шов **без единой правки тела** — у него уже были ровно эти три члена.
Это и был признак, что шов проходит по существующей линии, а не выдуман.

`CombatPresenter` берёт кадр, долю и «продвинуть время» у роутера; прямая ссылка на ленту осталась
только у боевых вещей (диспетчер событий, dev-оверлеи) и уедет в 1б.

Роутер и держатель зарегистрированы в `WorldLifetimeScope` — они переживают бои. Привязка ленты пока
случается на буте и живёт всю сессию, поэтому **поведение игры шагом 1а не меняется**; смысл появится
в 1б, когда боевой скоуп начнёт рождаться вместе с боем, а `BattleStageBinder` переедет в него как
есть.

## Готчи, которые всплыли по дороге

- **Арена регистрируется дважды** — в `WorldLifetimeScope` и снова в `CombatLifetimeScope`, при том
  что комментарий в первом утверждает обратное. VContainer отдаёт ближайший, поэтому дефект был
  невидим.
- **`ILPP`-диалог Unity** (`Can't find file \\.\pipe\unity-ilpp-…`) блокирует компиляцию в редакторе
  и не виден `compile-check.ps1` — тот собирает Roslyn'ом мимо редактора. Если редактор ведёт себя
  странно, сперва рестарт.
