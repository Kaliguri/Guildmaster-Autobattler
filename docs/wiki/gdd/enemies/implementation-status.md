---
title: "Enemies - Implementation Status"
order: 2
status: living
updated: 2026-07-29
---

# Противники: что в движке, что на бумаге

Отвечает на один вопрос: **какой враг существует в игре, а какой только описан.**
Дизайн — в карточках, принципы ростера — в [[enemies/index|Enemies - Catalog]].

## Как читать статус

Владелец факта — поле `impl` в шапке карточки; здесь сводка. Значения — те же три, что у реликвий
(см. [[gdd/relics/implementation-status|Relic - Implementation Status]] §Как читать статус).
Поле `asset` — id ассета (`enemy.goblin_grunt`).

**Мерка сверки:** наличие ассета в `Assets/_Project/ScriptableObjects/Enemies/` (виды —
`ScriptableObjects/Species/`). Ручная сверка — 2026-07-29: **4 карточки из 13** в движке, из видов —
только гоблины.

**Обновление 2026-07-31:** стат-блоки заведены для всех 13 карточных врагов (`impl: partial` у
девяти новых — стат-блок в движке, навыки на бумаге до Фазы 2), созданы виды `species.bandits`,
`species.beasts`, `species.golems` и AI-пресеты фракций. Механика (эффекты/способности/новый код —
слепота, антихил, швырок союзника) — Фаза 2. Ход: `docs/enemies-implementation-progress.md`.

**Фаза 2, пассивки (2026-07-31):** в движке механика пятерых — гоблин-воин (барьер 50 / 1 заряд /
кд 4 с), гоблин-убийца (маскировка средней ступени + `+50%` в тыл), разбойник со щитом (барьер 100 /
2 заряда / кд 6 с), отравленный арбалет (яд + антихил 50%), волк стаи (`+15%` за союзника в радиусе
3). Трое из них — `impl: engine`; щитоносец и арбалетчик остаются `partial` из-за побега (ниже).
**Антихил стал общей шкалой** — `effect.antiheal_weak|medium|strong|absolute` (30/50/75/100%) поверх
`HealShieldTakenEff`, доступна любому киту. Ждут Фазы 2: командир, маг, наездник, брузер, варлок,
голем (у всех механика — способности), плюс швырок союзника и слепота (новый код).

## Расхождение: побег разбойников при 30% HP

Карточки трёх разбойников (Шаг 6) обещают «при `30%` HP пытается сбежать по общему правилу фракции».
В движке есть **AI-порог отступления** (`_retreat` в профиле: отход на своей половине и возврат при
восстановлении) — фракция играбельна и читается как «побежал», но **выхода из боя совсем нет**: юнит
не покидает арену и не забирает с собой золото, о котором говорит карточка. Отдельная механика
«сбежал = снят с арены» не заведена; пока это осознанное приближение, а не реализованный замысел.

## Расхождение: HP элит гоблинов ↔ каскад

Видовой множитель `species.goblins` MaxHP ×0.4 (для «серой массы») умножает ФИНАЛ стата
(`Stats.cs`: `(base+Flat)×(1+PctAdd)×Π(1+PctMult)`), поэтому override HP на юните им срезается —
элитам гоблинов не пробить видовую хрупкость ни через Override, ни через Flat. Итог класс×вид:
командир 800, маг 520, наездник 600. Карточка наездника заявляет «HP МНОГО» — **сейчас движок это
не даёт**. Костыль «override = желаемое/0.4» отвергнут (хрупок к смене множителя). Чистые пути —
решение Макса/balance: элитный подвид с компенсирующим PercentMult, пересмотр видового множителя,
или отдельный HP-путь для элит. До решения элиты на честных класс×вид числах.

## В движке

```dataview
TABLE asset AS "Ассет", impl_note AS "Расхождение"
FROM "gdd/enemies"
WHERE impl = "engine" OR impl = "partial"
SORT order ASC
```

## Только дизайн

```dataview
TABLE status AS "Статус дока"
FROM "gdd/enemies"
WHERE impl = "paper"
SORT order ASC
```

## Виды

| Вид | Состояние |
|---|---|
| [[gdd/enemies/species/goblins\|Goblins]] | в движке — `species.goblins` |
| [[gdd/enemies/species/bandits\|Bandits]] · [[gdd/enemies/species/beasts\|Beasts]] · [[gdd/enemies/species/golems\|Golems]] | только дизайн |

Все четыре бандита имеют карточки, но ни ассета врага, ни ассета вида — то есть **фракция бандитов
целиком на бумаге**. Это самая крупная готовая-к-реализации пачка контента.

## Чего требуют боссы от движка

Разнесено из карточек боссов 2026-07-30 (правило «карточка держит только механику», журнал
2026-07-30/1). Все три босса — `impl: paper`.

**Общее для всех трёх.** Многостадийность **швом не является**: по каркасу 2026-07-29 стадия — обычный
бой, связь между стадиями сюжетная, нужен только контейнер узла
([[gdd/enemies/bosses/index|Enemies - Bosses]]).

### [[gdd/enemies/bosses/boss-deserter-army|Дезертировавшая Армия]]

| Требование | Есть? | Комментарий |
|---|---|---|
| Чтение состава игрока и выдача баффов от него | нет, но дешёвый | нужен «профиль команды» (главный тип урона, роли, связки). **Не боевой шов:** чистая функция от ростера, считается до первого тика и складывает статмоды — детерминизм не трогает |
| Катсцена перед расстановкой с подстановкой прочитанного | нет | лок-строка с параметром, не фиксированный текст; порядок «катсцена → расстановка → бой» обязателен |
| Статистика профиля за все забеги (стадия 2) | нет | статистика забега считается на месте, история профиля — новое поле сейва; тот же заход, что счётчики поведения игрока ([[gdd/00-meta/roadmap\|Roadmap]] §Блок 2.5) |
| Перенос состояния между стадиями | не шов | это данные: во второй стадии стоит другая запись Тактика, уже с баффами |
| Воскрешение и вывод юнита из боя | нет, но дешёвый | путь `DeathSystem` (снятие со spatial hash и из активных), только без смерти |
| Неуязвимость по порогам HP | частично | иммунитет как эффект есть; образец порога уже применяется — `PassiveThresholdPct * MaxHP` в `BulwarkComponent.cs:103` |
| Смена атмосферы по стадии | частично | слои есть (свет, пост, аудио-шины), связки «стадия → атмосфера» нет |
| Доп. тип урона + сопротивление ему | да | школы урона и защиты есть |

### [[gdd/enemies/bosses/boss-goblin-warband|Гоблинская Банда]]

| Требование | Есть? | Комментарий |
|---|---|---|
| Две задачи одновременно | дешёвый | две зоны размещения с квотой 2 + 2, проверка до боя. Зоны есть, нужны квота и валидация; ядро не трогается |
| Зона размещения кольцом (стадия 2) | нет | формы зон прямоугольные — тех-долг взят сознательно |
| Союзные/нейтральные NPC | не нужно | големы — обычные враги команды босса; третья сторона снята решением про две зоны |
| Барьеры — Стена | спроектировано | [[gdd/20-combat/barriers\|дизайн]] + [[tech/40-planning/barriers\|план]] |
| Массовое отбрасывание с рестартом расстановки | частично | смещение есть; окно перерасстановки между стадиями принято каркасом, не реализовано |
| Наказание за кучность (молния по кластеру) | частично | «Проводимость» есть как свойство эффекта, но не как заявленный рычаг |
| Щит в 300% max HP | да | обычный щит, вопрос чисел |

### [[gdd/enemies/bosses/boss-bandit-nest|Разбойничье Гнездо]]

| Требование | Есть? | Комментарий |
|---|---|---|
| Подкрепление посреди боя («догоняет в спину через X сек») | нет | враги сейчас все на арене со старта |
| Выбор направления побега перед боем | нет | выбор, влияющий на арену и расстановку |
| Ветвление сценария от того, **кто** погиб | нет | требует, чтобы исход боя нёс данные, а не только «победа/поражение» |
| Диалоговая система с таймером | нет | простая диалоговая система числится отдельной идеей |
| Побег врага при низком HP | частично | AI-порог отступления есть, «выйти из боя совсем» — нет |
| Снижение награды как следствие решения | частично | экономика наград есть, «модификатор награды от сценария» — нет |

## Ассеты без карточки

`enemy.training_dummy` — дев-болванка для арены отладки и балансных бенчей. Карточки не имеет
и не должна: это инструмент, не противник забега.

## Энкаунтеры

Восемь ассетов `Encounters/` (`GoblinAmbush`, `GoblinRaid`, `GoblinScouts`, `GoblinSkirmishLine`,
`GoblinWarband` + три `Dummy*`) карточек в ГДД **не имеют вовсе**: состав встречи — это баланс,
а не дизайн-сущность, и живёт он в бенчах контура `balance`. Если появится дизайн-язык встреч
(«какие бывают волны»), его дом — [[combat-system|Combat - System]], не эта папка.
