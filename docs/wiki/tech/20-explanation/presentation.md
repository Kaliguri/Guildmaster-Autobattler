---
title: "Explanation - Presentation"
order: 50
status: archive
updated: 2026-07-16
---

> [!warning] Архив: описание кода на дату сверки, а не правда о коде
> С 2026-07-30 правда о коде живёт **в коде** (`<summary>`/`<remarks>`) и в тестах: у каждого факта
> один владелец, выбранный по тому, что ломается при расхождении. Страница не пополняется и с кодом
> не сверяется — имена классов, сигнатуры и структура могли измениться. Читать как замысел и
> историю; актуальное искать в коде. Почему так —
> [[tech/00-meta/journal/2026-07-30-code-owns-truth-journal-owns-why|Journal - Code Owns Truth, Journal Owns Why]].

**Было при заморозке:** Ready — отражает код на 2026-06-19

---

> «Тело» игры: как состояние симуляции превращается в картинку, и почему визуал плавный при том, что сим тикает всего 30 раз в секунду.
>
> Связано с [[tech/20-explanation/index|Explanation - Code Map]], [[tech/20-explanation/simulation|Explanation - Simulation & Tick]], [[tech/20-explanation/di-events|Explanation - DI & Events]].

---

## 1. Простыми словами

Симуляция считает бой 30 раз в секунду и держит «голые числа»: у юнита №5 позиция (3.2, 1.0) и 320 HP. Экран обновляется 60+ раз в секунду и должен показать это красиво: спрайт на месте, HP-бар, при ударе — цифра урона и вспышка.

**Презентация только читает.** Она смотрит на состояние сима и рисует его. Она **никогда** не меняет бой. Это железное правило: если бы вью двигал юнита, мы бы потеряли детерминизм и рассинхронили «мозг» с «телом».

---

## 2. `CombatPresenter` — мост сим→визуал

`Assets/_Project/Scripts/Presentation/CombatPresenter.cs`. `MonoBehaviour`, получает `CombatSimulation` и MessagePipe-паблишеры через `[Inject]`-метод ([[tech/20-explanation/di-events|Explanation - DI & Events]]).

Что делает:
1. **Подписывается** на C#-события сима (`OnUnitSpawned`, `OnUnitDied`, `OnDamageDealt`, `OnBattleEnded`).
2. На спавн — инстанцирует `UnitView` и привязывает к `RuntimeUnit` (`Bind`); кладёт в словарь `id → view`.
3. На урон/смерть — дёргает вью (вспышка, цифра урона, анимация смерти).
4. **Ретранслирует** каждое событие в MessagePipe (`DamageDealtEvent`…), чтобы Audio/VFX/UI подписывались независимо.

```csharp
private void HandleUnitDied(RuntimeUnit unit) {
    if (_views.TryGetValue(unit.Id, out var view)) {
        view.OnDeath();
        _views.Remove(unit.Id);
    }
    _unitDiedPublisher.Publish(new UnitDiedEvent(unit));
}
```

Связь по `unit.Id` (а не по ссылке на объект) — потому что вью отслеживает юнита по стабильному идентификатору, и это переживёт будущую сетевую репликацию.

### 2.1. Направленный импакт-фидбэк: право и направление

Часть фидбэка **имеет сторону** — искры разлёта, отброс тела цели, выпад атакующего, пыль под ногами. Правило на них одно и держится в `HandleDamageDealt`.

**Право.** Направленный фидбэк рисуется только на `DamageResult.IsDirectHit` (авто-атака или способность). У тика яда, горения и у ответки шипов брони нет ни автора в моменте, ни стороны, а приходят они так же часто, как удары: искры на них читались бы как «тебя ударили», когда никто не замахивался. Что дотам оставлено — цифра урона и вспышка цели: обе не врут про направление. Свой визуальный язык для периодического урона — открытый дизайн-вопрос ([[gdd/10-vision/visual-direction|Vision - Visual Direction]], трек 5).

**Направление.** Мили берёт вектор «бьющий → цель». Дальний берёт **траекторию снаряда**, а не этот вектор: пока снаряд летит, стрелок успевает сместиться (у Следопыта для стрельбы на ходу есть отдельный флаг в данных), а цель — шагнуть вбок, и «откуда прилетело» перестаёт совпадать с «где стоит стрелок».

**Готча, определившая реализацию:** снаряд к моменту события урона уже помечен мёртвым — сим ставит ему точку удара и `IsAlive = false` в том же тике, что и наносит урон. Спросить у него направление на месте не у кого, поэтому презентер запоминает курс каждого выстрела **по цели** (`_lastShotDir`), а список живых снарядов держит отдельно (`_projById`). Оба словаря чистятся в `HandleBattleReset`: в новом бою id юнитов свои.

Один удар — один вектор: то же направление уходит и в искры, и в отброс, и в выпад. Расхождение между ними читается как два разных удара.

---

## 3. Интерполяция: 30 Гц сим → 60+ fps рендер

Если рисовать юнита ровно в его тиковой позиции, на 60 fps он будет «телепортироваться» раз в два кадра (сим обновляет позицию 30 раз/с, экран — 60). Чтобы движение было плавным, вью **интерполирует** между прошлой и текущей тиковой позицией.

Для этого `RuntimeUnit` хранит **две** позиции: `PreviousPosition` (где был на прошлом тике) и `Position` (где сейчас). `MovementSystem` в начале своего шага записывает `PreviousPosition = Position`, потом двигает `Position`. Вью рисует точку между ними:

```
визуальная позиция = lerp(PreviousPosition, Position, alpha)
```
где `alpha ∈ [0,1]` — «насколько мы продвинулись внутри текущего тика».

`CombatPresenter.Update` раздаёт `alpha` всем вью каждый кадр; `UnitView.UpdateInterpolation(alpha)` двигает спрайт.

> 🔎 **Починено (док был старше кода).** `alpha` берётся у петли — `CombatPresenter.Update` читает `_interpolation.Alpha`, то есть остаток аккумулятора из `CombatLoopService`. Прежняя формула `Time.deltaTime / TickDelta` считала не долю шага, а отношение кадра к тику: при стабильном fps это константа, и тела вечно стояли в одной и той же промежуточной точке вместо движения между тиками.

---

## 4. Почему именно так

- **Read-only презентация** = детерминизм цел, «тело» можно переписать (другой рендер, другой UI) не трогая «мозг».
- **Интерполяция вместо высокочастотного сима** = дёшево. Считать бой 60 раз/с вдвое дороже, а визуально то же самое даёт интерполяция поверх 30 Гц.
- **Мост в MessagePipe** = Audio/VFX/UI не знают про симуляцию; добавить новый подписчик (например, экранную статистику) можно, не трогая ни сим, ни презентер.

> Это прямое следствие большого решения проекта — разделять симуляцию и презентацию. Именно оно позволяет «телу» быть толстым и «юнитёвым» (Feel-хуки, partikли, твины LitMotion), а «мозгу» оставаться чистым и сетевым. Подробнее о том, почему это решение правильное для нашего жанра — [[tech/20-explanation/netcode|Explanation - Netcode]].

---

## Итог документа

- Презентация только читает состояние сима; никогда его не меняет.
- `CombatPresenter` — мост: подписка на C#-события → вью + ретрансляция в MessagePipe.
- Плавность даёт интерполяция между `PreviousPosition` и `Position` (сим 30 Гц, рендер 60+).
- Текущая формула `alpha` — грубая, помечена в техдолг (взять долю из аккумулятора loop).
