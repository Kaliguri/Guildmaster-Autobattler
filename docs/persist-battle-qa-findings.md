# Persist-бой (Ф2) — находки боевого play-QA

Контекст: боевой play-QA Макса после сборки Ф2 (persist-поток боя, ветка `feat/persist-battle-flow`,
коммиты `23c56062` / `da49224b` / `137065d2`). Дата: 2026-07-18.

Легенда критичности: **P0** — блокер/вылет, чинить первым · **P1** — важный геймплей/UX · **P2** — визуал/полиш · **P3** — мелочь/косметика.

---

## A. Вылет из play при клике режима «Бой» на ивенте/карте — **P0 (блокер)**

**Симптом:** на экране ивента (и, вероятно, карты) нажатие «Бой» в топбаре → play-mode выключается.
Лог: `[ActRunner] - выбран недоступный узел 'null' → Aborted` + `MissingReferenceException FloatingText`
(вторичное, при teardown play).

**Корень (найден, это регрессия от persist-скоупа Ф2 — мой):**
`DeploymentController.Start()` (строка 101) на старте ставит `_session.SetPhase(BattlePhase.Fighting)`.
Раньше combat-скоуп был per-battle → вне боя контроллера не было → `Phase == None`. Теперь скоуп
**persist** (живёт с бута) → `DeploymentController.Start` срабатывает раз на буте → **`Phase = Fighting`
навсегда, даже вне боя**. Из-за этого guard `onBattle` в `UiRootBootstrap.InitTopBar:174`
(`if (_clock.Phase != BattlePhase.None) CloseOverlays()`) больше не защищает: он всегда пропускает →
`CloseOverlays` → `CloseAll` сносит flow-модалку ивента/карты → её `DetachFromPanelEvent` резолвит
выбор узла как `null` → `ActRunner` Aborted → вылет.

**Двойная защита нужна:**
1. Фаза боя должна корректно отражать состояние в persist-скоупе (вне боя `Phase = None`,
   не `Fighting`). `DeploymentController` не должен объявлять Fighting на буте. Связано с фазой
   расстановки (см. B) — фаза должна выставляться при ВХОДЕ в бой, не в `Start`.
2. Системный: flow-модалки (карта/ивент/сундук/шоп) не должны сноситься мод-переключением в null.
   Известный «готча-родственник» из прошлого (см. память, #2). Мод-кнопки при открытой flow-модалке
   = no-op, а не CloseAll.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA). Убрал `SetPhase(Fighting)` из `DeploymentController.Start`
— теперь вне боя `Phase = None` (дефолт), фаза выставляется по факту (Deployment/Fighting/None). Мой
guard onBattle (`Phase != None`) снова защищает: на ивенте/карте `Phase == None` → «Бой» = no-op, flow-
модалка не сносится. Системный фикс #2 (flow-модалки в принципе не сносить) пока не делал — guard достаточен.

---

## B. Нет обязательной фазы расстановки перед боем — **P1 (ключевой геймплей)**

**Симптом:** вход в бой-узел → бой начинается СРАЗУ. Нет стадии, где видно противников, можно
расставить/переставить отряд под них и лишь потом нажать «Начать».

**Корень (моё упущение развилки в Ф2.4):** launch (`BattleBootstrap.LaunchBattle`) доспавнивает врагов
и сразу снимает паузу (`SetPaused(false)`). Free-расстановка перед боем (`FreeDeploymentRequested` →
`DeploymentController.OnFreeDeployment`) больше не поднимается.

**Нужно:** вход в узел → доспавн врагов + **пауза** + фаза Deployment (враги видны, отряд можно
двигать, камера управляется) → кнопка «Начать» снимает паузу → бой. Обязательная стадия.
Переработать launch: не `SetPaused(false)`, а войти в Deployment; `RequestStart`/«Начать» стартует бой.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA). `LaunchBattle` теперь: доспавн врагов + `SetPaused(true)` +
`EncounterLoader.RequestDeployment(preset)` → поднимает существующий `FreeDeploymentRequested` →
`DeploymentController.OnFreeDeployment` входит в фазу расстановки (пауза, враги видны, drag отряда, «Начать»).
Переиспользован рабочий Free-поток (узел уже строит preset как `DeploymentMode.Free` с ростером из RunState,
`NodeResolver.cs:82`) — `DeploymentController` не переписывался. «Начать» → `StartCombat` → unpause + Fighting.
Ретрай тоже через расстановку. **PLAY-QA: проверить что перед боем видно врагов, отряд двигается, «Начать» стартует.**

---

## C. Тинты визуалов не из одного источника — **P2 (визуал, старый долг #3)**

**Симптом:** тинты юнитов некорректные; должны идти из ОДНОГО источника (обсуждалось в прошлых сессиях).

**Корень:** долг #3. Единый источник тинта (`UnitVisualResolver`) был снесён в P1 редизайна лоадаута;
сейчас тинт живёт в `CombatPresenter.TintFor` (HSV по хэшу имени), а грид-карточки/roster шли своим
путём → рассинхрон. План был пересобрать единый резолвер в P5/#3.

**Нужно:** восстановить `UnitVisualResolver` (чистый helper `(UnitData,team)→{tint,flipX}`), все
потребители (боевой презентер + грид карточек + любой preview) зовут ОДИН источник. Детали — изучить
прошлые обсуждения #3 при фиксе.

**Статус:** не исправлено. P2.

---

## D. Нет общего заднего фона для не-боевых экранов — **P2 (UX/визуал)**

**Симптом:** persist-камера рендерит серую тест-арену fullscreen вне боя → за главным меню, картой
акта, ивентом, сундуком видна голая арена. Странно смотрится. Нужен общий backdrop для не-боевых
экранов (меню/карта/ивент/сундук/…).

**Корень:** прямое следствие persist-мира (камера всегда рендерит арену; P2-готча была отмечена).
Раньше вне боя был тёмный фон Background Camera.

**Нужно:** общий задний фон (арт/градиент/атмосфера) под не-боевыми экранами — либо fullscreen-подложка
UITK за меню-стеком, либо скрывать/затемнять арену вне игрового вида. Дизайн-задача (арт кладёт Макс).

**Статус:** не исправлено. P2.

---

## E. Камера не двигается / не меняет режим вне боя — **P2 (связан с B)**

**Симптом:** вне боя нельзя двигать камеру и переключать её режим.

**Корень (гипотеза):** вне боя нет активного режима расстановки/боя → нет активного камера-ввода
(`BattleInputController`/deployment-ввод не в контексте). Камера-риг persist есть (P2), но управление
им не подключено вне боя.

**Нужно:** камера-ввод (пан/зум/режим) доступен в фазе расстановки. Вероятно решится вместе с B
(обязательная фаза расстановки активирует камера-ввод). Проверить после B.

**Статус:** не исправлено. P2, зависит от B.

---

## F. MissingReferenceException FloatingText при остановке play — **P3 (teardown-косметика)**

**Симптом:** при выключении play — `MissingReferenceException: FloatingText has been destroyed` в
`CombatPresenter.EnsureTextPool b__42_4` (`CombatPresenter.cs:388`), из `ObjectPool.Clear` на teardown.

**Корень:** `actionOnDestroy` пула дёргает `ft.gameObject` у уже уничтоженного объекта при выходе play.

**Нужно:** null-чек в `actionOnDestroy` пула FloatingText (`if (ft != null) ...`). Косметика, только
при остановке play.

**Статус:** ИСПРАВЛЕНО. `CombatPresenter.cs:388` — `actionOnDestroy: ft => { if (ft != null) Destroy(ft.gameObject); }`
(Unity-оверлоад `!=` ловит уже уничтоженный объект при teardown play).

---

## G. Material MAT_Sprite_HitFlash не поддержан 2D SRP Batcher — **P3 (перф-warning)**

**Симптом:** `Material 'MAT_Sprite_HitFlash' has _TexelSize / _ST texture properties which are not
supported by 2D SRP Batcher. SRP batching will be disabled for 2D Renderers using this Material.`

**Корень:** материал hit-flash использует свойства, несовместимые с 2D SRP Batcher → батчинг
отключается для 2D-рендереров с этим материалом (микро-перф).

**Нужно:** убрать `_TexelSize`/`_ST` из шейдера hit-flash или пометить совместимо. Не срочно.

**Статус:** ОТЛОЖЕНО ОСОЗНАННО (не правлю вслепую). Кандидат-фикс: в `SH_Sprite_HitFlash.shader`
поменять `half4 _MainTex_ST` → `float4 _MainTex_ST` в обоих CBUFFER (2D SRP Batcher требует float4-layout
для `_ST`). НО это боевой ВИЗУАЛЬНЫЙ шейдер (тинт/флип/вспышка) — правка вслепую рискует сломать рендер
спрайтов; проверить уход warning можно только в play (visual-QA Макса). P3 микро-перф — делать аккуратно
вместе с Максом, не ради галочки.

---

## Порядок фикса (рекомендация)

1. **A (P0)** — вылет, блокирует любое тестирование забега.
2. **B (P1)** — обязательная фаза расстановки; заодно чинит E (камера).
3. **E** — проверить после B.
4. **C, D (P2)** — визуал (тинты, задний фон).
5. **F, G (P3)** — мелочи под уборку.

A и корень B — следствия моего Ф2-захода (persist-скоуп сломал фазовую логику; убрала расстановку).
Беру на себя.

---
---

# РАУНД 2 — play-QA после A+B+D (2026-07-18)

Второй боевой прогон Макса. 15 находок — камера, деплой-визуал, карты, топбар, поток.

## 1. «Бой» вне боя не открывается — а договаривались о серой тест-арене — **P1**
Мой фикс A сделал «Бой» вне боя = no-op (чтобы не ронять flow-модалку). Но по дизайну «Бой» вне боя
должен показать СЕРУЮ ТЕСТ-АРЕНУ с отрядом (закрыть оверлеи, показать мир), а не ничего. Нужно развести:
«Бой» закрывает не-flow оверлеи (инвентарь) и показывает арену, но НЕ роняет flow-модалку (карта/ивент).
Развилка: как отличить «показать арену» от «уронить flow». Кандидат: закрывать только инвентарь-оверлей,
а на flow-экране «Бой» = no-op (или мягкий выход, если flow это допускает).

## 2. В инвентаре камера не двигается / не переключается — **P1**
Управление камерой в режиме инвентаря не работает. Гипотеза (связь с #7): UI-режим глушит геймплейный
ввод (`IInputService.GameplaySuppressed = true` в `MenuRouter.EnterMenuMode`), камера-ввод завязан на него.
Найти где блокируется, развязать камеру-ввод от UI-suppress (камера должна жить под инвентарём-оверлеем).

## 3. Добавить пан камеры зажатой СРЕДНЕЙ кнопкой мыши (MMB) — **P2**
Новая фича ввода: MMB-drag = пан камеры. Через `IInputService`/`CameraModeController`.

## 4. Стадия расстановки: камера должна быть НЕ Action, а свободная (двигать можно); и она ОГРОМНАЯ — **P1**
Два в одном: (а) в Deployment камера = свободный режим (Overview/pan-zoom), не Action-слежение;
(б) начальный размер/позиция сейчас ОЧЕНЬ большие (отзумлена на всю арену) — должен быть стартовый
кадр как у Action-камеры (тот же размер/место), просто управляемый. `CameraModeController` — стартовый
режим/зум при входе в Deployment.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `4f09aa1`). `CameraModeController.EnterDeployment(center, spread)`
— переключает в Overview (свободный пан/зум в пределах зоны), но стартовый кадр = центр+разброс боя
(как Action-камера, `spread + _actionZoomPadding`), НЕ `MaxZoomForZone` (вся зона). `ExitToActionView()`
на старте боя возвращает слежку. `DeploymentController` инъектит риг и на входе в расстановку считает
центр/разброс по позициям ВСЕХ живых юнитов (свои + враги — видно противника), без гонки с focus-таймером.
**PLAY-QA: камеру в расстановке можно двигать/зумить, стартовый кадр — на бой, не на всю зону.**

## 5. Единый источник для unit-карт (анимации): у реликвий красиво, в сундуке — нет — **P2 (#3 расширенный)**
Карты юнитов в лоадауте анимированы (`RelicCardVisualRig`), а в других местах (сундук/награда) — нет.
Нужен ЕДИНЫЙ источник того, как рисуется/анимируется unit-карта, и все потребители (лоадаут/сундук/
награда) зовут его. Расширение долга #3 (не только тинт — весь визуал карты).

## 6. В инвентаре у реликвий НЕТ тинта (просил ранее) — **P2 (#3)**
Тинт не применяется к картам реликвий в инвентаре. Долг #3 (единый `UnitVisualResolver` снесён в P1).
Пересобрать источник тинта и подключить к инвентарным картам.

## 7. В режиме инвентаря боевая сцена не реагирует — даже юнитов не подвинуть — **P1 (связан #2)**
UI-режим глушит ВСЁ управление (юниты + камера). Гипотеза: `GameplaySuppressed` при открытом UITK-оверлее.
Но инвентарь = прозрачный оверлей поверх арены → под ним хочется двигать юнитов/камеру. Развязать:
ввод над «дыркой»/прозрачной зоной инвентаря идёт в мир (deployment-ввод), над панелями — в UITK
(паттерн `panel.Pick`). Ядро проблемы камеры+юнитов в инвентаре.

## 8. Некорректная зона перетаскивания юнита (слишком большой круг) — **P2**
Pick/hover-зона юнита завышена (большой круг на скрине). `DeploymentController.PickUnit`/`BodyRadius` —
подогнать зону под реальный силуэт юнита.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `00625307`). «Большой круг» = ховер-кольцо строилось по
`max(extents.x, extents.y)` AABB спрайта → высокий спрайт (рост 1.7) раздувал радиус по высоте. Заменено на
тень-эллипс у ног радиусом `BodyRadius` (см. #9). **PLAY-QA: ховер-подсветка у ног, не гигантский круг.**

## 9. При ЛКМ — странный центральный круг вместо контура под ногами; тащить должны ВСЕГО юнита (призрак) — **P2**
Деплой-визуал: (а) выделение = мелкий центральный круг, а нужна окантовка ТЕНИ под ногами;
(б) при drag тащится «кружок», а должен — полупрозрачный призрак ВСЕГО юнита (копия силуэта).
`DeploymentView.SetOutline/SetGhost` + `DeploymentController` drag-визуал.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `00625307`). Развилки Макса: призрак = статичный кадр,
тень = нейтральная тёмная. (а) кольцо-outline заменено на сплющенную тень-эллипс у ног. (б) ghost-диск →
полупрозрачная копия текущего кадра спрайта юнита (`UnitView.BodySprite`/`BodyFlipX`/`BodyLossyScale`/
`BodyWorldPosition`, тот же flip/scale, зелёный/красный тинт по валидности drop), следует за курсором, под
ним та же тень. Headless/нет-вида → только тень. **PLAY-QA: при drag виден силуэт юнита + тень, не кружок.**

## 10. Не отображается зона расстановки ПРОТИВНИКА (добавить, полезно) — **P3**
`DeploymentView` рисует только player-зону. Добавить отрисовку enemy-зоны (визуальный ориентир).

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `00625307`). `DeploymentView.BuildZoneBorder` больше не гейтит
`Side == Player` — рисует обе стороны, вражьи зоны красноватой рамкой (свои зелёные, Extended голубой).
**PLAY-QA: видна и вражья зона расстановки.**

## 11. Табы топбара: только «Инвентарь» подсвечивается активным; «Карта»/«Бой» — нет — **P2**
`SetActiveMode` вызывается только для инвентаря (`UiRootBootstrap.Update:214`). Карта/Бой не получают
active-состояние. Подсвечивать активный режим по факту (карта открыта → «Карта» active, бой → «Бой»).

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `6ab10bea`). `UiRootBootstrap.ActiveMode(phase)`: инвентарь >
бой/расстановка (Phase!=None) > read-only карта (флаг `_mapOpen`). NB: карта петли акта (обход узлов) идёт
через флоу, не через бутстрап — её подсветка потребует шва флоу→топбар (не покрыто). **PLAY-QA: «Бой» и
«Карта» (кнопка) подсвечиваются активными.**

## 12. Кнопка меню настроек (справа ☰) и ESC — должны ОТКРЫВАТЬ настройки, а не закрывать окна — **P1**
Сейчас ☰/ESC = `ToggleSystemMenu` (на одном экране → CloseAll). Ожидание: ☰ открывает меню настроек
(pause/settings) поверх, не закрывая текущее. ESC — аналогично (открыть меню, не «назад/закрыть вслепую»).

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `6ab10bea`). `ToggleSystemMenu`: если pause в стеке — шаг назад
(settings→pause→закрыть), иначе Push(pause) ПОВЕРХ текущего (инвентарь/карта/бой). Заодно чинит готчу-родственника
#2: карта петли акта больше не рушится в null от CloseAll по ☰/ESC. **PLAY-QA: ☰/ESC открывают меню поверх,
не закрывают инвентарь/карту.**

## 13. Карта акта не имеет заднего фона — **P2 (проверить с D)**
Макс отметил отсутствие фона у карты. NB: скрин РАУНДА 2 показывает тёмный фон за картой — вероятно
backdrop (D) уже лёг. Возможно: (а) скрин до D-коммита, либо (б) хочет полноценный АРТ-фон, не голый
тёмный surface-sunken. Уточнить: устраивает ли тёмный backdrop как база (арт добавит Макс) или нужен
отдельный фон карты сейчас.

## 14. Кнопка «Начать» — чуть ниже панели, по центру; потом ТАМ ЖЕ её заменяет таймер боя — **P2**
Позиционирование: «Начать» — по центру, чуть ниже топбар-панели. В бою в ТОЙ ЖЕ точке — таймер.
`RunModeBarView` центр-слот (сейчас `SetFighting`/`HideBattleCenter` управляют центром) — вынести
ниже панели, единая точка «Начать»↔таймер.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `6ab10bea`). `btn-start`+`battle-timer` вынесены из правой зоны
в `battle-center` — absolute-слот по центру X, `top: var(--gm-topbar-height)` (чуть ниже панели). `SetFighting`/
`HideBattleCenter` находят их по имени, свап «Начать»↔таймер в ТОЙ ЖЕ точке. **PLAY-QA: «Начать»/таймер по
центру под панелью.**

## 15. Слишком большой отступ между названием гильдии и «Возвышением» — **P3 (полиш)**
`RunModeBar` левая зона (`Alebardium` / `ASCENSION · IV`) — уменьшить вертикальный спейс между строками.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `6ab10bea`). `.gm-loadout__guild-name` `margin: 0`,
`.gm-loadout__guild-asc` `margin: 0; margin-top: -2px` — строки подтянуты. **PLAY-QA: спейс убран.**

## 13. Карта акта не имеет заднего фона — **P2 (проверить с D)** → закрыто D
Карта акта идёт при `Phase == None`, где backdrop от D (`ee7e5ff7`) включён (`showBackdrop = Phase==None && !inventoryOpen`).
Значит фон под картой уже есть (тёмный surface-sunken). Полноценный АРТ-фон — задача Макса (арт кладёт он).

---

## РАУНД 2 — план по группам (порядок на согласование Макса)

**Группа I — топбар/навигация (быстро, низкий риск, видимый эффект):** #11 активные табы, #12 настройки
открывают, #14 «Начать»/таймер по центру, #15 спейс гильдия-возвышение. + #13 проверить/уточнить фон карты.

**Группа II — камера и ввод в UI (ЯДРО юзабилити, связаны):** #7 (UI глушит ввод) + #2 (камера в инвентаре)
— общий корень (suppress/panel.Pick). #4 (Deployment-камера свободная + стартовый кадр). #3 (MMB-пан).

**Группа III — деплой-визуал:** #8 зона pick, #9 контур-тень + drag-призрак, #10 зона врага.

**Группа IV — единый визуал unit-карт (#3):** #6 тинт в инвентаре, #5 единый источник анимаций карт
(лоадаут/сундук/награда). Пересбор `UnitVisualResolver` + общий rig-путь.

**Группа V — поток:** #1 «Бой» вне боя → серая тест-арена (развести показ арены vs снос flow-модалки).

Рекомендованный порядок: **II → III → V → I → IV** (сначала оживить управление в расстановке/инвентаре —
это блокирует твоё тестирование; потом деплой-визуал; поток «Бой»; быстрый топбар-полиш; крупный визуал
карт последним). Либо **I первым** как быстрый видимый прогресс, если хочешь размяться.

---

## ДОЗАПИСЬ (2026-07-19, после II.1/II.3 play-QA)

Макс проверил #7/#2/#3 — развязка ввода **работает** (юниты/камера под инвентарём, клики по панелям не
протыкают, ESC глушит). Новое:

### 16. Драг юнитов в расстановке НЕ работает — **P1 (связан #9)**
После развязки ввода клики проходят, но **перетаскивание юнитов не работает** (юнит не тащится). Отдельно
от panel.Pick. Копать `DeploymentController` drag-цикл (`OnPointerPressed`→`_dragged`→`Tick`→`OnPointerReleased`):
почему drag не стартует/не двигает. Возможно связано с #9 (визуал ghost) и/или с тем, что pick по спрайту
не находит юнит. Требует разбора drag-пути целиком.

**Уточнение симптома (Макс):** drag в ЧИСТОЙ расстановке (без инвентаря) РАБОТАЕТ. Не работает — **только
с открытым инвентарём** («вообще не реагирует»). Значит это не сам drag-цикл, а глушение ввода инвентарём.

**Корень (найден):** `LoadoutInventoryView.Build` (стр. 46) насильно ставил `root.pickingMode = Position`,
перезаписывая `Ignore` из UXML. Корень инвентаря покрывает ВЕСЬ экран → `panel.Pick` над боевой «дыркой»
(battle-zone) возвращал панель, а не null → `PointerOverUI == true` под всем инвентарём → `OnPointerPressed`
(гейт `!PointerOverUI`) не фаерился → деплой-драг не стартовал. Камера жила, т.к. не завязана на Pick.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `4f09aa1`). Вернул `root.pickingMode = Ignore` (как в UXML) —
над дыркой Pick = null (ввод в мир), непрозрачные `.gm-loadout__main` ловят pick сами (Position по дефолту).
**PLAY-QA: при открытом инвентаре юнитов над боевой «дыркой» (левая зона) можно перетаскивать.**

### 17. Всё ещё нет тест-зоны боя вне забега — **P1 (= раунд-2 #1, повтор)**
Подтверждение приоритета: «Бой» вне боя должен показывать **серую тест-арену** с отрядом (потестить бой/
расстановку без реального забега), а не no-op. Это группа V #1 — НЕ сделано. Макс повторил → делать.

### 18. В ESC-меню (в игре) нет кнопок «Вернуться в меню» и «Выйти из игры» — **P2**
`MenuRouter.BuildPauseScreen` (сейчас только «Продолжить»/«Настройки») — добавить «В главное меню»
(сбросить забег → `GameFlow.RunGameAsync`/меню) и «Выйти из игры» (`Application.Quit`/выход из play).
Loc-ключи + проводка во flow.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `8a048d61`, 395/395). Развилка Макса: полный CancellationToken.
Новый шов `IRunControl` (Core) — `GameFlow` реализует. «В главное меню» отменяет per-акт `CancellationTokenSource`
→ висящие await'ы петли (выбор узла/«Продолжить»/исход боя) бросают `OperationCanceledException` → `RunGameAsync`
ловит и уходит на главное меню (сейв остаётся — можно продолжить). Токен несёт `RunContext` (не менял сигнатуры
каждого флоу); сигнатуры +ct только у `IMapNodeChooser.ChooseAsync`/`IContinuePresenter.WaitForContinueAsync`;
`BattleFlow` уже принимал токен. «Выход» → `GameFlow.QuitGame`. `PauseScreen.uxml` +2 кнопки (loc `ui.menu.to_main_menu`/
`.quit`, RU-фолбэк). **PLAY-QA: в бою/на карте ESC→меню→«В главное меню» возвращает в главное меню; «Выход» закрывает.**

---
---

# РАУНД 3 — play-QA после топбара I + #18 (2026-07-19)

Третий прогон Макса. 4 находки — z-order/оверлеи, читаемость расстановки, консистентность подсветки, баг «В меню».

## 19. Кнопка «Начать» должна быть ЗА другими UI + ESC-меню не должно прятать оверлеи под ним — **P1**
Два в одном: (а) `battle-center` («Начать»/таймер) сейчас в топбаре (BringToFront) — должна быть в САМОМ НИЗУ
z-order, ЗА инвентарём/меню/картой (перекрывается ими). (б) ESC-меню (pause) при открытии ПРЯЧЕТ экран под ним
(`Push` ставит `_stack.Peek().style.display = None`) — оверлеи «перестают отображаться», хотя должны быть видны
за pause (открываются обратно на Pop). Нужно: pause НЕ прячет нижний экран (виден за ним).

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `19414736`). (а) `UiRootBootstrap.InitTopBar` репарентит `battle-center`
из топбара в корень + `SendToBack` (под оверлеи, над backdrop) — топбар остаётся BringToFront, «Начать» уходит вниз.
(б) `Push(hideBelow)` — pause открывается с `hideBelow:false`, нижний экран остаётся `display:Flex`, виден за
scrim-фоном pause. **PLAY-QA: «Начать» перекрывается инвентарём/меню; ESC-меню поверх, инвентарь/карта видны за ним.**

## 20. Расстановка нечитаема: нужен круг «размера» под КАЖДЫМ юнитом + реакция на ховер/drag — **P2 (расширение #8/#9)**
Под каждым юнитом всегда должен быть круг, показывающий его «размер» на поле (не только на ховер). Плюс явная
реакция: круг РЕАГИРУЕТ на наведение (навёлся на персонажа) и на начало перетаскивания. Сейчас тень только на
ховер/drag одного юнита. Добавить читаемости: базовый круг-размер под всеми team-0 всегда + hover/drag-состояния.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `19414736`). `DeploymentView` пулит круги-кольца: под каждым живым
team-0 юнитом кольцо радиусом `BodyRadius` (эллипс «на полу») ВСЕГДА; наведённый — ярче+толще (реакция); drag-юнит
скрыт из колец (он «в руке» — ghost-силуэт следует за курсором = реакция на drag). `DeploymentController.UpdateUnitRings`
собирает состояния покадрово. **PLAY-QA: под юнитами круги-размеры, ярче на наведении, начало drag читается.**

## 21. Подсветка активного таба: бой/инвентарь ОК, а КАРТА — нет; нужна КОНСИСТЕНТНОСТЬ — **P1 (uitk)**
«Бой» и «Инвентарь» подсвечиваются активными корректно, а «Карта» (петли акта) — нет. Макс: всё должно работать
из ОДНОГО источника, консистентно, без таких казусов — и в принципе, не только этот баг. Корень: `ActiveMode`
в `UiRootBootstrap` знает про инвентарь (флаг) и бой (Phase), но карта петли акта открывается через флоу
(`MapScreenNodeChooser`→`MenuRouter`), мимо бутстрапа → флаг `_mapOpen` не ставится. Нужен ЕДИНЫЙ источник
активного режима — стек `MenuRouter` (каждый оверлей несёт mode-тег), топбар читает верхний тег.

**Уточнение (Макс):** речь именно про **selected-состояние кнопки-таба** (подсветка активного режима), не про фон/рендер.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `19414736`). Единый источник — стек `MenuRouter`: `Push(screen, mode)`
кладёт mode-тег в `userData`; `IMenuRouter.ActiveScreenMode` возвращает тег верхнего оверлея. `OpenInventory`→`"inventory"`,
`OpenMap`→`"map"` (обе карты — read-only И петли акта — через один `OpenMap`, теперь подсвечиваются). `UiRootBootstrap.ActiveMode`:
`router.ActiveScreenMode ?? (Phase!=None ? "battle" : null)`. Убран флаг `_mapOpen`. Дефолт: при наложении selected =
верхний оверлей. **PLAY-QA: таб «Карта» подсвечивается активным (и просмотр, и обход узлов), консистентно с боем/инвентарём.**

## 22. «Выйти в меню» выкидывает из play-mode вместо главного меню — **P1 (баг #18)**
Логи: «прерываю забег» → «акт завершён: Aborted» → «выход из игры» (QuitGame). **Корень — реэнтрантный
`CloseAll`:** на карте клик «В меню» → `CloseAll()` удаляет map-экран → его `DetachFromPanelEvent`-страховка
СИНХРОННО резолвит выбор узла в null → `ActRunner` Aborted → `RunGameAsync` крутит виток → открывает главное
меню ВНУТРИ ещё идущего `CloseAll` → тот же `CloseAll` (итерирует по мутирующемуся стеку) удаляет свежий
mainmenu → его detach-страховка шлёт `Quit` → `QuitGame` → выход из play. Фикс: атомарный `CloseAll`
(snapshot + clear СТЕКА до detach-каскадов) — разрывает реэнтрантность. `WaitOutcomeAsync` уже реагирует на
токен, так что «В меню» в бою идёт через cancel-path.

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `19414736`, 395/395). `CloseAll` теперь снимает снапшот стека и
очищает его ДО удаления экранов → реэнтрантный `Push` (свежее главное меню) переживает `CloseAll`, его detach
не срабатывает вхолостую. Диагноз по логам: «прерываю забег»→«акт Aborted»→«выход из игры». **PLAY-QA: «В главное
меню» в бою И на карте возвращает в главное меню, не роняет play.**

---
---

# РАУНД 4 — play-QA после раунда 3 (2026-07-19)

Четвёртый прогон. 5 находок. #1/#3/#4 исправлены (`5216452b`); #2/#5 — крупные, на развилках.

## 23. Кнопка «Начать» видна прямо в главном меню — **P1 (баг #19)**
После репарента `battle-center` из топбара в корень (#19) она отвязалась от скрытия топбара, а `SetFighting`/
`HideBattleCenter` вызывались только в run-active ветке (за early-return) → в главном меню «Начать» торчала над меню.
**Статус:** ИСПРАВЛЕНО (`5216452b`). Видимость battle-center теперь управляется ЯВНО каждый кадр: показ только когда
идёт забег И `Phase != None` (Deployment/Fighting), иначе скрыт. **PLAY-QA: в главном меню «Начать» нет.**

## 24. Круг НЕ под ногами (в центре) + зона хватания слишком большая — **P2 (доработка #20/#8)**
Круг-размер рисовался в центре спрайта, а зона pick = весь AABB спрайта (значительно больше круга).
**Статус:** ИСПРАВЛЕНО (`5216452b`). Круги якорятся к `UnitView.FeetPoint` (ноги, не центр); `PickUnit` тестит круг
у ног радиусом `BodyRadius × 1.3` (чуть больше рисуемого круга), убран AABB-спрайт pick. **PLAY-QA: круг под ногами,
зона хватания ≈ круг.**

## 25. Круг не светится при хватании + не под ногами призрака — **P2 (доработка #4/#9)**
Перетаскиваемый юнит был скрыт из колец — не видно, кого тащишь. Ресёрч (TFT-паттерн): footprint-круг = единый язык.
**Статус:** ИСПРАВЛЕНО (`5216452b`). Круг перетаскиваемого остаётся светиться у ног ПРИЗРАКА (следует за курсором),
цвет по валидности drop (зелёный/красный) — видно кого тащишь и можно ли поставить. Плоская тень-диск убрана (круги
несут всю читаемость). **PLAY-QA: при хватании круг под призраком светится, цвет = валидность.**

## 26. «Бой» вне забега → серая тест-арена (= #17, повтор) — **P1, НА РАЗВИЛКАХ**
Макс: «Бой» вне боя должен открывать серую тест-арену с отрядом (потестить бой/расстановку без реального забега).
Конкретика: перекрасить текущие тайлы арены через наш grayscale-инструмент (Palette Remapper) → «серая зона».
Сверяться с [[persist-world-decisions]] (НЕ делать «Бой» вне боя пустышкой).

**Решения Макса:** отряд = текущий/демо, БЕЗ врагов (полигон для расстановки/реликвий/чтения). Арт grayscale — делать сейчас.

**ЛОГИКА СДЕЛАНА (`625af4b1`, 395/395, ждёт play-QA):** `ToggleTestZoneRequest` (Data) — «Бой» вне реального боя
= тумблер тест-зоны. `DeploymentController.EnterTestZone` строит слоты из УЖЕ стоящих team-0 юнитов (WorldStage их
поставил), входит в расстановку без врагов; «Бой» ещё раз → `ExitTestZone` (Phase→None). `UiRootBootstrap.OnBattleMode`:
в бою → боевой вид; иначе тумблер, но НЕ поверх flow-экрана (карта петли/ивент/шоп — снос уронил бы забег), только
чисто/инвентарь. Это заодно разблокирует #5 вне боя (расстановка активна). **ОГРАНИЧЕНИЕ:** нужен стоящий отряд
(забег начат); демо-отряд для теста из главного меню — след. шаг.

**АРТ grayscale СДЕЛАН (`63dec900` ассеты + `08b7077e` скин).** Оценка сцены: арена = Cainos-тайлсет (2 тайлмапа
Grass/Wall, 179 простых Tile). Скриптом: desaturate 2 текстуры + перенос нарезки (64+61 спрайт) → 125 grayscale-тайлов.
WorldScene получил grayscale-дубль пола (Grid дубль, 1252 клетки swap на серые тайлы, выключен). `TestZoneArenaSkin`
(colorRoot/grayRoot) свапает по `ToggleTestZoneRequest` — цвет в бою, серый в тест-зоне. Выбран two-root свап, НЕ
дубль всего пайплайна тайлсета (оценка объёма). **PLAY-QA: в забеге вне боя «Бой» → серая арена + расстановка (без
врагов), можно двигать/эквипить реликвии; ещё раз «Бой» → цветная арена + выход.**

## 27. Реликвию из инвентаря НЕ перенести на персонажа + нет визуала «в руках» — **P1, НА РАЗВИЛКАХ (= план Ф3b.5 / долг #5)**
Drag карточки реликвии из UITK-грида инвентаря на world-юнита = эквип. Ф1-мост `EquipRelicAtCursorRequest` (подписчик
`DeploymentController.OnEquipAtCursor` ЕСТЬ, публикатор НЕТ — мой шов). Плюс визуал «в руках» (ghost при drag). Есть
`UiDragDrop` (UITK pointer+capture). Развилки: (а) визуал «в руках» (ghost карточки / иконки реликвии / силуэта юнита),
(б) дроп-таргет (только юнит в battle-zone / любая точка).

**Решения Макса:** визуал «в руках» = **призрак силуэта ЮНИТА** (не карты), единый стиль с drag юнита, «меняется в одном месте».

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, `2179b405`, 395/395). `UnitSilhouette` — ЕДИНЫЙ источник ghost-силуэта:
из живого `UnitView` (drag юнита) ИЛИ из `RelicData.ViewPrefab` (drag реликвии, юнита на поле нет); одна отрисовка
`DeploymentView.SetGhost`. Новое `RelicDragEvent` (Start/Move/Drop) — грид публикует, фаза расстановки берёт позицию
курсора из своего ввода (событие несёт лишь реликвию+фазу). `LoadoutInventoryView` вешает pointer-capture drag на
карту (порог клик/drag, клик по-прежнему выбирает). `DeploymentController` рисует силуэт реликвии у курсора + кольцо
цели, на Drop надевает (`EquipOn`). **ОГРАНИЧЕНИЕ:** работает в фазе расстановки (`_deploying`); эквип на тест-арене
вне боя — с #26. **PLAY-QA (в расстановке боя): тащить карту реликвии на юнита → силуэт «в руке» + эквип.**

---
---

# РАУНД 5 — play-QA после #2/#5 (2026-07-19). NB: РЕШЕНО ресёрчить ВЕСЬ UI системно — точечные латки плодят баги.

Пятый прогон. 4 находки. НЕ чинились (Макс: контекст большой, следующий заход = ресёрч UI-архитектуры целиком). #28/#29 — доработки; #30/#31 — регрессии UI-координации.

## 28. Трава на сцене не перекрашена в grayscale — **P2 (доработка #26)**
Grayscale-скин перекрасил тайлмапы пола (Grass/Wall), но НЕ декор-траву: ~70 `PF Grass A-O` SpriteRenderer'ов
(`TX Plant.png`) + возможно трава в тайлах. В тест-зоне трава остаётся цветной. Нужно: grayscale-версия `TX Plant`
+ swap спрайтов декор-травы в grayscale-дубле (или в TestZoneArenaSkin). НЕ начато.

## 29. Хваталка юнита: не чистый круг, а круг + часть ВЫШЕ (рекоменд. размер спрайта); мульти-юнит → ближайший к кругу — **P2 (доработка #24)**
Сейчас `PickUnit` = круг у ног `BodyRadius×1.3`. Макс: зона хватания должна быть кругом-опорой ПЛЮС частью выше
(до рекомендованного размера спрайта — вертикальный прямоугольник/капсула над кругом). При нескольких юнитах под
курсором drag выбирает того, к чьему КРУГУ (ногам) мышь ближе. `DeploymentController.PickUnit` — расширить зону
вверх (круг + бокс высотой ~recommended sprite) + тай-брейк по расстоянию до центра круга. НЕ начато.

## 30. Инвентарь СНОВА сломан — камера и юниты не реагируют — **P1 РЕГРЕССИЯ (UI-координация)**
Опять не взаимодействовать ни с камерой, ни с юнитами в режиме инвентаря (как было до развязки #7/#2). Вероятный
корень: тест-зона (#2) правит suppress/context ВРУЧНУЮ (`HideTopForTest` снимает `GameplaySuppressed`; `EnterTestZone`
ставит context Deployment), и это конфликтует с тем, как инвентарь глушит/развязывает ввод (`SyncSuppress` +
panel.Pick). Режимы наложились. Блокирует тестирование. Разбирать в UI-ресёрче.

## 31. «Бой» много раз → серая/зелёная/странная арена → ВЫХОД ИЗ ИГРЫ — **P0 РЕГРЕССИЯ (мой вход-с-карты `a892ec21`)**
Стектрейс: `OnBattleMode:298` (ветка инвентаря) → `CloseOverlays` → `CloseAll` сносит MAP-экран → его detach-страховка
`resolve null` → `ActRunner` Aborted → `RunGameAsync` → главное меню detach → `Quit` → выход из play. **Корень —
рассинхрон `_testActive` + стек роутера:** тумблер тест-зоны (`OnBattleMode`) НЕ учитывает, что поверх тест-зоны
открылся инвентарь. Стек `[map(скрыта HideTopForTest), inventory]`, `_testActive` рассинхронен с фактическим
состоянием; при `mode=="inventory"` ветка зовёт `CloseOverlays` → `CloseAll` сносит ВЕСЬ стек, включая скрытую под
тест-зоной карту → Aborted → Quit. Мельтешение цвета (серая↔зелёная↔странная) = многократный toggle skin/deploy
без согласования состояния. **Это корневая болезнь: тест-зона как ещё один режим поверх стека роутера + ручной
suppress/context = комбинаторный взрыв состояний.**

## === ВЫВОД РАУНДА 5: РЕСЁРЧ UI-АРХИТЕКТУРЫ ===
UI-координация переусложнилась: **стек роутера (MenuRouter) + suppress/context (IInputService) + режимы топбара
(инвентарь/карта/бой) + тест-зона (Phase + скрытие карты + ручной suppress) + panel.Pick развязка** — слишком много
пересекающихся состояний, каждая точечная правка (#12 меню-поверх, #21 mode-теги, #2 тест-зона, вход-с-карты)
добавляет режим и ломает соседний. Регрессии #30/#31 — прямое следствие. **Следующий заход: НЕ латать точечно, а
провести ресёрч всего UI-слоя как системы** — MenuRouter стек, suppress/context-модель, режимы навигации, тест-зона,
как они должны сосуществовать. Спроектировать единую модель состояний UI (одна машина состояний вместо флагов
`_testActive`/`_inventoryOpen`/`_hiddenForTest`/`_menuModeActive`/mode-тегов), потом переписать. Файлы UI-слоя:
`MenuRouter`, `UiRootBootstrap`, `InputService` (suppress/PointerOverUI), `RunModeBarView`, `DeploymentController`
(тест-зона/расстановка), `TestZoneArenaSkin`.

---

# === РЕВОРК UI, ПАКЕТ П1 (Ф0-Ф3) — play-QA Макса 2026-07-19 ===

Контекст: play-QA после влития Ф0-Ф3 UI-реворка (`1a36bf0f`/`bced833a`/`7756b948`/`168f747f`,
ветка `feat/persist-battle-flow`). Легенда критичности прежняя. **Часть находок — прямые следствия
костылей, которые ещё не снесены (снос в Ф4-Ф5); их НЕ латать точечно, чинить структурно в своей фазе.
См. реестр костылей в `docs/ui-architecture-rework-progress.md`.**

## 32. ESC/системное меню доступно в ГЛАВНОМ меню — **P2 (→ Ф4)**
Системное меню (pause по ESC/☰) открывается и в главном меню. Должно быть **только в игровом лупе (забеге)**.
Корень: `UiRootBootstrap.OnMenuToggle` → `_router.ToggleSystemMenu` не гейтится наличием забега (ESC всегда-он).
Фикс: гейтить открытие системного меню по `RunStateService.Current != null` (или «верхний экран не главное меню»).
Отнести к Ф4 (слой топбара/меню).

## 33. Настройки: высота панели зависит от контента (табы разной высоты) — **P3 (→ Ф4 / Трек Д)**
Панель настроек меняет высоту в зависимости от выбранного таба (Игра/Графика/Звук — разное кол-во контента).
Макс: сделать ОДНУ фиксированную максимально-высокую высоту (не прыгает при смене таба). Мелочь, но записать.
Фикс: `min-height`/фикс-высота панели настроек в USS. Отнести к Ф4 или Треку Д (дизайн-система).

## 34. «Бой» вне боя: 1-е нажатие удаляет тест-бой, 2-е возвращает (тумблер рассинхрон) — **P1 (→ Ф5)**
Вне боя клик «Бой» в топбаре: первое нажатие УДАЛЯЕТ старую тест-зону, второе — возвращает. Логика тумблера
рассинхронена. Корень — `_testActive` флаг в `UiRootBootstrap.OnBattleMode` + самотогл `TestZoneArenaSkin`
(костыли, см. реестр). Это ровно зона Ф5 (тест-зона = Sheet-экран + единый источник `IBattleSession.TestZone`,
скин слушает состояние, не тумблер). НЕ латать — чинить в Ф5.

## 35. Табы: подсветка странно взаимодействует с ESC (будто переключает «Бой»/«Карту») — **P1 (→ Ф4)**
При открытии/закрытии ESC-меню подсветка активного таба топбара скачет (кажется, переключает бой↔карту).
Макс: «будто костыли в коде, надо найти причину». Гипотеза корня: подсветка `ActiveMode(phase)` в бутстрапе
берёт `ActiveScreenMode ?? фаза` — когда pause (ModeTag=null) сверху, подсветка падает на фазу, прыгает.
Разобрать вместе с моделью слоёв в Ф4 (единый источник подсветки не должен зависеть от pause поверх).

## 36. ESC-меню НЕ блокирует топбар — табы кликабельны при открытом меню — **P1 (→ Ф4). МНЕНИЕ МАКСА (записано):**
Сейчас топбар держится поверх всего (`BringToFront` каждый кадр — костыль), поэтому табы кликабельны даже
при открытом ESC-меню. **Решение Макса (2026-07-19, ОТМЕНЯЕТ принятое ранее QA #12 «топбар кликабелен над меню»):**
- Табы видны **всегда В ЗАБЕГЕ**, работают над обычными экранами (карта/инвентарь/магазин).
- НО ESC-меню (системное меню) — **превыше всех**. Оно **ЗАТЕМНЯЕТ ВСЁ, включая топбар** (fullscreen-scrim
  поверх топбара) и **блокирует клики** (в т.ч. по табам), перехватывая pointer.
- **НЕ СКРЫВАТЬ** топбар/табы — именно затемнять и не давать нажать (визуально остаются, но неактивны/тусклые).
Реализация — Ф4 (слои): Modal-экраны (pause/settings) кладутся в слой ВЫШЕ топбара; Page/Sheet — под топбаром.

## 37. «В меню» из паузы во время карты → `[ActRunner] выбран недоступный узел 'null' → Aborted` — **P1, МОЯ Ф3-зона**
Стектрейс: `MenuRouter.BuildPauseScreen b__48_1:303` («В меню») → `CloseAll()` → `PopAll` →
карта (`RouterResultScreen<string>`) `ResolveDefaultIfPending` → `Resolve(null)` → `MapScreenNodeChooser`
получает null → `ActRunner:54` «выбран недоступный узел null → Aborted» → `GameFlow` «акт завершён: Aborted».
**Корень:** `btn-main-menu` делает `CloseAll()` (синхронно резолвит карту дефолтом null → Aborted) ДО/вместе с
`RequestReturnToMainMenu()`. Два механизма прерывания забега конфликтуют: снос экрана через PopAll(null) vs
отмена CTS забега. Правильно: «В меню» отменяет забег через CTS, а отмена CTS сама закрывает карту через `ct`
(ShowAsync принимает ct, но `ShowMapAsync` его НЕ пробрасывает — надо пробросить актовый ct до навигатора),
и это должно быть Cancelled, а не Aborted-выбор-null. Затрагивает `MenuRouter.ShowMapAsync`, цепочку ct
`GameFlow`/`ActRunner`/`MapScreenNodeChooser`, `IRunControl.RequestReturnToMainMenu`. **Приоритетный фикс.**

**Статус:** ИСПРАВЛЕНО В КОДЕ (ждёт play-QA, 408/408). Единая система отмены забега (согласовано с Максом: за
единую модель + подтверждено ресёрчем — канонический cooperative-cancellation паттерн, снос K11+K12). Токен
забега (`RunContext.Cancellation`, уже был) пробрасывается через ВСЮ цепочку показа: запросы `OpenMap/Continue/
Reward/Shop/Chest/TextEvent` несут `CancellationToken`; источники (`MapScreenNodeChooser`/`ContinuePresenter` уже,
+`RewardPresenter`/`ShopFlow`/`ChestFlow`/`TextEventFlow`) вешают `AttachExternalCancellation`; `UiNavigator.Push`
получил `ct`-оверлоад (снимает простой экран при отмене; `ShowAsync` уже умел). «В меню» теперь = `Pop()` паузы +
`RequestReturnToMainMenu()` — БЕЗ `CloseAll`-веника; отмена сама закрывает открытый экран забега и всплывает
`OperationCanceledException` в `GameFlow.RunGameAsync` → главное меню (сейв цел). `ActRunner` различает отмену
(`IsCancellationRequested` → `ThrowIfCancellationRequested`) от реального недоступного узла. Родственный баг
«Продолжить» (тоже висел на `CloseAll` → на карте снёс бы её) — тоже на `Pop`. Регресс-тест
`ActRunnerTests.RunAct_CancelledDuringNodeChoice_ThrowsOperationCanceled_NotAborted`. **PLAY-QA: ESC→меню→«В
главное меню» с карты И из узла (награда/магазин/сундук/ивент) возвращает в главное меню без вылета; «Продолжить»
на карте не роняет забег.**

## 37b. Запрос Макса: детальный лог Aborted-пути — **P2 (диагностика)**
«Уже который раз вылетает, не хватает детального лога что произошло». Улучшить диагностику пути прерывания:
логировать ПОЧЕМУ узел null (закрытие/отмена/В меню), кто инициировал (ESC/кнопка/CTS), — чтобы Aborted
читался из лога без раскопок стектрейса. `ActRunner`/`GameFlow`/`MapScreenNodeChooser`.

**Статус:** ИСПРАВЛЕНО (вместе с #37). `ActRunner` теперь логирует отдельно: отмену выбора узла («выбор узла
отменён (выход из забега) → OperationCanceled, не Aborted») и реальный тупик (Aborted-лог обогащён: текущий узел,
кол-во доступных, явная пометка «это НЕ отмена — реальный тупик/баг данных»). Плюс `GameFlow` уже логировал
«забег прерван из меню → возврат в главное меню» (теперь этот путь реально срабатывает). Aborted vs Cancelled
читаются из лога без стектрейса.

---

# РАУНД 6 (2026-07-26) — заход Макса по давним багам (ветка feature/unit-tag-icons)

## 38. Конец боя мгновенно закрывается непрозрачным фоном, добивания не видно — **P1. ИСПРАВЛЕНО (511/511)**
**Симптом (Макс):** «в конце боя что-то скрывает бой бэкграундом, а так быть НЕ должно».
**Корень (две беды в одной точке):**
- `BattleFlow.Run` звал `IBattleSession.RequestReset()` СРАЗУ после исхода → `BattleBootstrap.ResetToWorld`
  чистил арену (враги прочь, отряд пере-поставлен) и ставил `BattlePhase.None`;
- `UiRootBootstrap.RefreshShell` показывает непрозрачный `.gm-screen-backdrop` ровно по `phase == None`.
Итого «2 секунды на досмотр добивания» (`BattleNodeFlow.postWinDelaySeconds`) шли за фоном и по пустому полю.
**Фикс:** новая фаза `BattlePhase.Aftermath` («бой кончился, игрок ещё на узле»): арена живая, фон запрещён,
топбар держит замерший таймер (не «Начать»), ввод — контекст `Combat` (камера жива). Фазу выставляет
`BattleBootstrap.OnBattleEnded`. Чистку арены забрал ВЛАДЕЛЕЦ УЗЛА — `BattleNodeFlow` зовёт `RequestReset()`
в `finally` (уход с узла любым путём: награда взята / поражение / отмена забега из меню), поэтому фаза не
залипает. Dev-разрез одного боя (`GameFlow.RunSingleBattleAsync`) чистит сам.
**Решение Макса:** арена видна ВЕСЬ бит после боя, включая экран награды.
Тесты: `BattleNodeFlowTests.Win_ResetsArena_OnlyAfterReward`, `.Defeat_ResetsArena_Too`.

## 39. Юнита можно схватить только за круг у ног — **P1. ИСПРАВЛЕНО**
**Симптом (Макс):** хотелось хватать по всему спрайту, а не по кругу-опоре.
**Фикс (реш. Макса):** пикинг двухслойный — круг-опора (`BodyRadius × 1.3`) ИЛИ AABB спрайта тела
(`UnitView.SpriteContainsWorldPoint`). Круг главнее спрайта: попадание в чей-то круг всегда бьёт попадание в
чужой спрайт (иначе высокий сосед перехватывал бы клик по ногам соседа). Внутри слоя — ближайший по ногам
(«хватаем круг ближайшего»). Тот же пикинг ловит дроп реликвии на юнита.

## 40. При взятии в руки круг-опора прыгает юниту в середину — **P1. ИСПРАВЛЕНО**
**Корень:** при drag круг рисовался прямо в точке курсора (`dragFeet = world`), а призрак-силуэт брал офсет
арта от СИМ-позиции (`UnitSilhouette.FromView(dv, _dragged.Position)`) — круг и силуэт жили по разным якорям.
**Фикс:** круг рисуется у ног ПРИЗРАКА (`целевая сим-позиция + _feetOffset`, где `_feetOffset` = «ноги минус
центр», снятый один раз в момент захвата — чтобы кадр анимации не ёрзал кругом), силуэт берёт офсет от
текущих ног живого вида.

## 41. Юнит «магнитится» центром под курсор при драге — **P1. ИСПРАВЛЕНО**
**Симптом (Макс):** «схватили за крайний левый угол — и ведём превью за крайний левый угол».
**Фикс:** `_grabOffset = unit.Position - world` снимается на захвате; целевая позиция = `курсор + _grabOffset`
(метод `DragTarget`). По ней же идут `CanPlace`/`Overlaps`/призрак/дроп — юнит на отпускании не прыгает.

## 42. Расстановка и надетые релики не переживают бой/этап — **P1. ИСПРАВЛЕНО**
**Корень:** `DeploymentController` правил ТОЛЬКО свои локальные слоты (превью боя). `RunState.Guild[i]`
(`SavedPosition`, `RelicId`) никто в рантайме не писал — `SavedPosition` заполнялся один раз в
`NewDefaultRun`. После боя `ResetToWorld → RosterDeployer.Deploy` поднимал отряд из RunState = откат к старту.
**Фикс:** слот расстановки знает свой индекс в гильдии (`Slot.GuildIndex`); дроп пишет
`RunStateService.SetSlotPosition`, экип на поле — `SetSlotRelic` (прямая посадка кита, минуя запас: инвентарь
сейчас показывает ВЕСЬ контент, а не запас забега — когда это сойдётся, путь схлопнется с `EquipRelic`).
На диск — на выходе из фазы (`StartCombat` / `ExitTestZone`), а не на каждый дроп.
**Связанное:** `GuildRoster.Resolve` больше не выбрасывает слот с ненайденным реликом (индексы гильдии
разъезжались) — сосуд встаёт с базовым китом и остаётся на своём месте.
**Решение Макса:** релик, надетый в расстановке, уходит в гильдию НАСОВСЕМ.
Тесты: `RunStateEquipTests.SetSlotPosition_PersistsIntoGuild`, `.SetSlotRelic_PutsKitOnSlot_WithoutTouchingStash`,
`.SetSlot_Fails_WhenSlotOutOfRange`, `GuildRosterTests.Resolve_FallsBackToBaseKit_WhenRelicMissing`.

**Все пять — ждут play-QA Макса.**

## 43. Ритм конца узла: передышка вместо гейта — **ПЕРЕДЕЛАНО по уточнениям Макса (510/510)**
Заход №38 закрывал фон, но ритм остался прежним (узел → блокирующая кнопка → карта). Макс уточнил модель:

**Как должно быть (реш. Макса 2026-07-26):**
1. Бой кончился → поле боя как есть (трупы, последний кадр), делей на досмотр.
2. Кнопка **«Продолжить»** (не «К наградам» — «пусть это и к ним, хд») → награда.
3. Последняя награда выдана = **узел засчитан**, игрок готов к следующему этапу. Петля НИЧЕГО не ждёт:
   `Advance` + автосейв сразу.
4. Игрок стоит в живом мире (**передышка**), в углу две кнопки — **«Продолжить»** (открыть карту) и
   **«К построению»** (встать в расстановку на боевой арене). Обе НЕ обязательны: те же места достижимы
   табами. Кнопки — в окончании ЛЮБОГО узла, не только боевого.
5. Карта акта открывается автоматом **только на входе в акт**; дальше — по кнопке/табу.

**Реализация:** фаза `BattlePhase.Aftermath` переименована в **`Interlude`** с более широким смыслом — «узел
пройден, игрок в живом мире» (держится до входа в следующий узел; фон UI запрещён на ней). Возврат мира и
кнопки передышки вынесены в шов `IRunBeatStage` (`RunBeatStage`): `EnterRestBeat` (вернуть мир + фаза
Interlude + кнопки) и `EnterNode` (мир на второй план → фаза None, под экраном узла снова есть задник).
`ActRunner` больше не ждёт «Продолжить»; `BattleNodeFlow` снова тонкий (чистка арены — не его дело).
`WorldMapController.BeginChoose(show)` умеет зажигать узлы, НЕ открывая карту. Кнопка «К построению» —
новый интент `SetFormationRequest` → `DeploymentController.EnterSandbox(grayZone: false)`: та же расстановка
без врагов, что и полигон, но арена остаётся боевой (цветной), а на выходе возвращается та фаза, из которой
вставали. Подписи кнопок — ключи `ui.beat.continue` / `ui.beat.formation` (таблица UI, RU заполнен).
Тест: `ActRunnerTests.RunAct_RestBeat_BetweenNodes_NotOnActEntry`.

**ЗАМЫСЕЛ НА БУДУЩЕЕ (записан по просьбе Макса, НЕ реализован):** возврат мира должен быть АНИМАЦИЕЙ —
трупы медленно тают, юниты воскресают и возвращаются на свои места (телепортом или пешком — не решено).
Кнопка «Продолжить» эту анимацию НЕ сбивает (уход на карту просто досчитывает её за кадром), а «К построению»
проигрывает её ускоренно (×3) и оставляет игрока «в состоянии боя». Сегодня возврат мгновенный
(`BattleBootstrap.ResetToWorld`), шов под скорость появится вместе с анимацией — там же стоит TODO.

## 44. Круг-опора и тень под ногами слишком мелкие — **ИСПРАВЛЕНО**
Реш. Макса: +45%. Круг — `DeploymentView.RingRadiusScale = 1.45` (радиус коллизии не трогаем, это визуал),
тень — `Feet Shadow` в `UnitView.prefab`, localScale ×1.45.

## 45. Зона хватания вышла гигантской — **ИСПРАВЛЕНО**
Заход №39 пикал по AABB спрайта, а у скелетного кадра он куда шире фигуры (замах оружия, прозрачные поля).
Теперь захват по ЭТАЛОННОМУ габариту (`UnitView._recommendedWidth × _recommendedHeight` от ног — та самая
зелёная рамка гизмо) + запас 0.08 мира: `UnitView.FigureContainsWorldPoint`. Не зависит от кадра анимации,
одинаков у всех юнитов и совпадает с тем, что игрок видит фигурой.

## 46. При перетаскивании круг исчезал с места юнита — **ИСПРАВЛЕНО**
Реш. Макса: круг на месте оригинала остаётся и горит ярко («тащишь именно меня»), а у ног призрака рисуется
второй — по валидности drop. `DeploymentController.UpdateUnitRings` кладёт для перетаскиваемого два круга.

---

# ПРИЁМКА 2026-07-26 (play-QA Макса)

Приняты без находок: **ряд тегов** на карточке юнита, **клик по узлу карты реальной мышью**
(выбор → бой → возврат на карту жив), весь **UI-чеклист сессии** (`docs/ui-qa-checklist.md`),
**ритм конца узла** (§43) — с двумя правками ниже.

## 43a. Передышка после НЕбоевого узла не должна пере-открывать арену — **ИСПРАВЛЕНО (510/510)**
**Симптом (Макс):** «если последнее что было — это не бой, не надо просто открывать бой; просто две кнопки».
**Корень:** `RunBeatStage.EnterRestBeat` звал `IBattleSession.RequestReset()` на КАЖДОМ стыке узлов, а
`BattleBootstrap.ResetToWorld` безусловно пере-расставлял отряд. После ивента/магазина/сундука/привала
убирать было нечего, и пере-расстановка на ровном месте читалась как «открылся бой».
**Фикс:** признак `BattleBootstrap._arenaStaged` — ставится в `LaunchBattle`/`RestartBattle`, снимается
возвратом; `ResetToWorld` при чистой арене — no-op. Признак ведём в боевом скоупе, а НЕ по типу узла в
петле: «?»-узел роллится в бой уже на входе (`RandomEventFlow`), и петля о его настоящем типе не знает.
Тестом не закрыт: `BattleBootstrap` зависит от конкретных `EncounterLoader`/`CombatSimulation`, гард
потребовал бы моков на классы — цена выше пользы для однострочного guard'а. Отмечено сознательно.

## 43b. Кнопки передышки — столбиком, а не строкой — **СДЕЛАНО**
Реш. Макса: «левую поставь под правую». `ContinueScreen.uxml` — порядок сменён (главная «Продолжить»
сверху, «К построению» под ней), `.gm-continue` переведён на `flex-direction: column`.
**Готча:** зазор повешен на класс самой второй кнопки (`.gm-continue__secondary`), а не на соседний
комбинатор `+` — USS его не поддерживает, правило молча не сработало бы; вдобавок в режиме гейта вторая
кнопка скрыта `display:none`, и её собственный отступ уходит вместе с ней (угол не съезжает).

---

# РАУНД 6 (play-QA Макса, 2026-07-26) — 5 находок

## 47. Затемнение перехода накрывает только карту, а должно — ВЕСЬ экран — **ИСПРАВЛЕНО (512/512)**
Клик по узлу гасил область карты, топбар и панели оставались светлыми.
**Корень:** шторка была мировым квадом (`WorldMapView`, материал `MapStyle.TransitionMaterial`) перед
камерой карты. Она честно гасила мир, но UI Toolkit рисуется ПОВЕРХ любых камер — накрыть его мировым
объектом нельзя в принципе.
**Фикс:** шторку рисует UI в новом верхнем слое `layer-transition` (`.gm-screen-fade`), поверх топбара и
модалок. Стадии по-прежнему ведёт карта (закрыть → засчитать выбор за чернилами → открыть) и шлёт плотность
через `ScreenFadeChangedEvent`.
**ЦЕНА, о которой надо знать:** UITK чужие шейдеры не умеет → переход стал ровным затемнением чернилами,
шейдерный узор `Guildmaster/Map/Transition` больше не участвует. Поле в `MapStyle` оставлено с пометкой;
если узор нужен — делать его картинкой-паттерном в USS. Тайминги in/hold/out работают как прежде.

## 48. Небоевой узел: кнопки передышки всё равно всплывают НА БОЕВОЙ АРЕНЕ — **ИСПРАВЛЕНО (512/512)**
Симптом (скрин 2): выбор в ивенте → экран ивента просто закрывается → под ним боевая арена с отрядом,
и на ней две кнопки. Мой фикс убрал лишнюю пере-расстановку, но не корень: **сам экран узла исчезает**,
и игрок проваливается в мир. Правильный ритм — см. #49: экран узла остаётся, кнопки ложатся на него.
**Фикс (вместе с #49):** экран узла живёт до входа в СЛЕДУЮЩИЙ узел, а кнопки бита переведены с
`ScreenKind.Page` на `Modal` — Page прячет всё под собой и стирал бы экран узла. Ввод при этом глушится
ровно как раньше (Page глушил так же), своего затемнения у экрана кнопок нет.

## 49. У ивента нет текста-исхода: выбор просто закрывает окно — **ИСПРАВЛЕНО (512/512)**
Требование Макса: выбор в ивенте → на том же экране остаётся **текст прощания/последствия** (что игрок
сделал и что произошло). И уже на этом же фоне, поверх него, появляются кнопки «Продолжить» /
«К построению». Экран узла живёт до входа в следующий узел, а не гаснет по клику.
**Корень:** механика текста-результата в `EventScreenView` БЫЛА, но пустой `ChoiceResultKey` означал
«закрыть сразу», а своя кнопка «Продолжить» звала `CloseAll` — второй, конкурирующий выход с того же экрана.
**Фикс:** (1) своей кнопки больше нет, у варианта без написанного исхода показывается общий текст-прощание
(новый ключ `ui.event.result.fallback`, RU «Вы двинулись дальше.»); (2) время жизни экрана — новый
`RunContext.NodeCancellation`: петля акта заводит токен на входе в узел и отменяет его, только когда игрок
вошёл в следующий. Два гарда: `RunAct_NodeScreenLives_UntilPlayerEntersNextNode`,
`RunAct_NodeToken_StillAlive_WhileItsOwnNodeRuns`.
**ДОЛГ:** сами тексты исходов у ассетов ивентов не написаны — сейчас везде фолбэк. Магазин/сундук/привал
пока живут прежним ритмом (закрываются сами) — переводить ли их на тот же, решает Макс.

## 50. Экран ивента на ЧЁРНОМ фоне вместо общего задника — **ИСПРАВЛЕНО (512/512)**
Скрин 1: под окном ивента сплошная чернота, тогда как в главном меню на том же месте живёт узор стола.
Фон должен браться из ОДНОГО источника — того же, что под главным меню. Никаких локальных заливок.
**Корень:** источников было два. Под меню презентация клала настоящий стол (`MenuBackdropView`, материал из
`MapStyle`), а под остальными экранами UI рисовал СВОЮ непрозрачную заливку `--gm-color-surface-sunken`.
**Фикс:** UI-заливка снесена целиком (`.gm-screen-backdrop` больше нет). UI только говорит, нужен ли фон
(`ScreenBackdropChangedEvent`), рисует его одно место — стол. Правило показа переехало с ФАЗЫ на СТЕК
(`UiNavigator.HasVisiblePage`): фаза врала бы, ведь экран пройденного ивента живёт и в Interlude.

## 51. Выход в меню не откатывает UI-состояние забега — **ИСПРАВЛЕНО (512/512)**
Повтор: вышел из забега → «Начать» новый → скрин 3: поверх карты открыт инвентарь, таб «Карта»
подсвечен, топбар в состоянии прошлого забега. Стек экранов/режимов переживает конец забега.
**Фикс:** показ главного меню начинается с `_nav.PopAll()`. Инвентарь, карта и тест-зона живут в стеке как
СОСТОЯНИЯ и своих владельцев переживали; `PopAll` резолвит висящие экраны дефолтом и через `OnExit`
обнуляет ссылки роутера, так что рассинхрона «экран снят, а владелец думает, что открыт» не возникает.

## 52. Единый ритм конца узла для магазина/сундука/привала — **СДЕЛАНО (512/512)**
Реш. Макса 2026-07-26 на вопрос «тот же ритм всем?»: да, единый. Магазин, сундук и привал больше не
сваливают игрока в мир по своей кнопке — узел сворачивается в **кадр-прощание** (та же панель, что у
ивента, без вариантов), который держит экран до входа в следующий узел; уводят с него кнопки бита.

Шов — `OpenNodeFarewellRequest(titleKey, bodyKey, ct)`: флоу узла публикует, `MenuRouter.ShowNodeFarewell`
рисует, живёт на `RunContext.NodeCancellation`. Текстовый ивент запрос НЕ шлёт — его кадр это его же
панель с текстом выбранного варианта (форма одна, текст у каждого узла свой).

Ключи (RU заполнен, EN прочерк): `ui.node.{shop,chest,camp}.title/.farewell`.
**Про тексты ивента:** исходы у `event.wandering_merchant` оказались УЖЕ написаны (все три варианта) —
дописывать было нечего. Фолбэк `ui.event.result.fallback` остаётся страховкой для будущих ивентов.

## 53. Переход не досчитывается, и у него пропал узор — **ИСПРАВЛЕНО (514/514)**
**Симптом (Макс, 2026-07-26):** «тьма до конца даже близко дойти не успевает». Требование: (1) быстрое
затемнение — **с текстурой**, (2) небольшая выдержка в полной темноте, (3) быстрый выход уже на новом
кадре. Будто моргнули. Плюс новое: не просто затемнение, а **вход в выбранную точку** — «зумимся в неё».

**Корень — владение, а не тайминги.** Три фазы вела карта акта (`WorldMapView.TickFade`), а выбор узла,
засчитанный на закрытом кадре, уводит игрока с карты: `EndChoose` → `Hide()` → `SetLayerActive(false)`,
где стоял сброс шторки (страховка из #47). Сброс срабатывал ровно на пике — выдержки и открытия игрок не
видел никогда. Владелец перехода обязан пережить смену того, что под шторкой.

**Фикс — переход стал самостоятельным (`IScreenTransition` / `ScreenTransitionRunner`):**
- три фазы живут в корневом скоупе, тикают по НЕмасштабированному времени и не зависят от заказчика;
- карта только заказывает моргание, получает `onClosing(0..1)` для наезда камеры и `onCovered` — на
  закрытом кадре засчитывает узел; `Hide` шторку больше не трогает;
- второй заказ поверх идущего игнорируется (у шторки один хозяин), `Cancel` открывает кадр немедленно.

**Текстура вернулась** (цена #47 отыграна): UITK чужие шейдеры не рисует, но картинку показывает —
материал `SH_Map_Transition` блитим в RT 360p (Point-фильтр, зерно дизеринга остаётся зерном) и кладём
фоном `.gm-screen-fade`. Шум переименован `_MainTex` → `_NoiseTex`: `Graphics.Blit` подменяет именно
`_MainTex`, и узор затирался бы белым квадратом. Материал переехал из `MapStyle` к UI-руту — у шторки
один владелец; в `MapStyle` остался только ритм.

**Вход «нырком»:** чернила схлопываются к ТОЧКЕ УЗЛА (`_Center` в долях экрана, `_Aspect` против овала,
нормировка на дальний угол — иначе сдвинутая точка закрывала полкадра рывком), узор подтягивается к ней
(`_Dive`), а камера в это же время наезжает на узел (`CameraModeController.DiveMapTo`, ручной пан/зум на
время нырка молчит). Кадр карты возвращается на закрытом кадре (`SurfaceMap`) — следующий показ карты
начинается с того вида, который игрок оставил.

**Ритм (ассет `MapStyle.asset`, не дефолты C#):** 0.28/0.08/0.36 → **0.30/0.18/0.26**. Выдержка была почти
нулевой — отсюда и «нет делея в темноте». Закрытие идёт с ускорением, открытие тормозит к концу.

Гарды: `ScreenTransitionRunnerTests` (6 тестов) — переход доживает до конца, когда заказчик ушёл на
закрытом кадре; подмена ровно раз и только на полной темноте; точка схлопывания едет от узла к центру.

### 53a. Доводка перехода по замечаниям Макса (2026-07-26)
Три правки после первого показа:

1. **Слишком быстро.** Ритм в ассете `MapStyle`: 0.30/0.18/0.26 → **0.45/0.25/0.40**. С наездом камеры прежние
   тайминги не давали движению прочитаться. Если покажется тягуче — резать выдержку, а не закрытие: цена
   шага заметна, а шагов за акт около четырнадцати.
2. **Форма смыкания — слот под рисунок.** Просьба была «сделать радиальную маску второй текстурой»; я
   возразила и предложила иное: радиал остаётся ПРОЦЕДУРНЫМ (ездит за точкой входа, остаётся кругом на
   любом аспекте — растянутой картинкой этого не добиться), а рядом появился слот `_ShapeTex` под
   НАРИСОВАННУЮ форму (клякса, спираль, рваный контур). Белое = закрывается последним; картинка садится
   серединой на выбранный узел, края — на самые дальние от него места кадра. Флаг `_UseShape` UI ставит
   сам по наличию текстуры (пустой слот в шейдере читается как чёрное, и без флага кадр закрывался бы
   разом). Реш. Макса: этот вариант.
3. **Узор не должен повторяться.** Не полный процедурный шум (FBM даёт «облака», а нам нужны мазки кистью),
   а ЖРЕБИЙ на каждое моргание: `ScreenFadeChangedEvent.Seed` — сдвиг, поворот и разброс масштаба выборки,
   плюс второй слой того же шума покрупнее искажает первый (`_Warp`). Одна текстура — рисунок каждый раз
   новый; сид держится постоянным весь переход. Случайность безопасна: переход — чистая презентация.

**Проверено (после того как сборка позеленела):** `ScreenTransitionRunnerTests` — 6/6, общий прогон
531/532. Единственный красный — `SmartStatStringTests.SameString_RendersShortAndDetailed` из чужой
параллельной работы над смарт-строками статов, к переходу отношения не имеет. Шейдер вдобавок проверен
офлайн-рендером: ход закрытия и три разных сида дают три разных рисунка.

### 53b. Нырок сильнее, чернила — следом (play-QA Макса, 2026-07-26)
1. **Зум слабый.** `CameraModeController._mapDiveZoom` 0.7 → **0.45** (кадр к концу нырка крупнее вдвое с
   лишним). Значение сериализовано в `WorldScene` на объекте `[Camera]`, а не оставлено дефолтом C#.
2. **Затемнение должно идти ПОСЛЕ зума.** Раньше шторка и наезд стартовали вместе, и нырок уходил под
   чернила. Теперь у формы перехода есть `InkDelay01` (доля фазы закрытия, ассет `MapStyle` = **0.4**):
   заказчик получает ход фазы СЫРЫМ и двигает камеру с первого кадра, а чернила вступают на 40% и
   отрабатывают остаток, приходя к полной темноте ровно к концу фазы.
   Вдобавок кривые разведены: наезд **тормозит** к концу (бросок в первых кадрах, пока экран чистый),
   закрытие по-прежнему **ускоряется**.

Гард: `ScreenTransitionRunnerTests.InkDelay_LetsTheDiveStartBeforeTheFrameDarkens` — при задержке 0.5 на
первой половине кадр не темнеет вовсе, а наезд уже идёт; к концу фазы шторка всё равно закрыта. 535/535.

### 53c. Ещё длиннее (play-QA Макса, 2026-07-26)
Второй заход по темпу: и нырок, и затемнение всё ещё «слишком быстро». Ритм в ассете `MapStyle`:
**закрытие 0.45 → 0.85с**, задержка чернил 0.4 → **0.35** (при более длинной фазе те же 0.3с чистого кадра
до затемнения), выдержка 0.25 → **0.30**, открытие 0.40 → **0.55**. Полный переход — **1.7с**.

**Цена, которую держим в уме:** узлов за акт около четырнадцати, то есть ~24с забега уходит в переходы.
Если начнёт ощущаться как ожидание — резать выдержку и открытие, закрытие с нырком оставлять длинным
(именно оно несёт движение).
