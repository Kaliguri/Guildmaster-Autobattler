# Витрина блума и стенд эффектов — журнал захода

**Временный файл, леса.** Сносится по закрытии захода. Правда о решениях уезжает в
`wiki/tech/00-meta/journal/` — там уже семь записей за 06.08.

## Исходная задача — НЕ ЗАКРЫТА

Макс: «все текущие блум эффекты слишком сильные». Симптом уточнён им: **слишком ярко / пересвет** и
**ореол большой, мылит**; «светится лишнее» — не жалоба. Мы третий час строим инструмент и громкость
блума так и не покрутили. Это главный незакрытый пункт.

Что уже известно по существу: весь свет живёт в коридоре вклада 0.8–2.2 (меньше полутора стопов),
вспышка смерти при этом самая тусклая из всех — то есть шкала перекошена независимо от громкости.
Правило ступеней записано (`code-standards` §9), лестница яркости НЕ сделана.

## Состояние стенда (Alebardium → VFX → Post FX Lab)

Сделано: один стенд вместо двух; один эффект за раз (12 штук) с перебором стрелками; юнит-линейка
сбоку; навигация мышью; проигрывание по НАСТОЯЩЕЙ длительности с темпом ×0.1…×2 и паузой; два таба
(Постпроцессинг / Эффекты); съёмка серий в лабораторию с манифестом; раздел лаборатории
«Свечение эффектов».

Починено по дороге (всё — настоящие баги игры, не стенда): четыре части тела не умели вспыхивать
(волосы, кисть, гарда, рукоять) + тест-сторож `BodyFlashMaterialTests`; звезда дробящего срезалась
краем меша; вес удара считался как самый тяжёлый в игре.

Заказ Макса: колющий пока 1в1 как снаряд (временно, «потом посмотрим»). Значения: сила свечения
каста 0.4, растекание блума 0.12.

## Шов для дуги — СДЕЛАНО (07.08, коммит `84eb03cf2`)

Стенд больше не пересказывает боевой путь, а зовёт его двумя публичными входами:

- `UnitView.ScrubSwing(clipTime)` — «поставь взмах сюда»; всё, что из этого следует (окно, удержанный
  кадр финишера, поток, номер взмаха, заказ дуги), решает вид;
- `SwingArcLaunch.Begin(...)` — единственный владелец чисел дуги; зовут презентер и стенд.

Стенд поднимает НАСТОЯЩИЙ `UnitView` (Bind с `BoneStorybookDevDuelist`), клип берёт из архетипа,
дугу заказывает через `SetSwingStartHandler`. Своего у него остался только сэмплер позы — в редакторе
Animator не тикает, и это механизм, а не логика.

Гейт: `SwingArcSingleOwnerTests` — `Begin` зовётся ровно из одного места, `ISwingArcSource` имеет
ровно одну реализацию. Прогон показа зелёный, 118/118.

**Сортировка** сделана по решению Макса («я меч, но чуть ниже меча»): дуга репарентится в носитель
`SortingGroup` тела и берёт order бьющей части минус один. Замер на префабе: родитель «Visual
Sprites», меч 7 → дуга 6. `VfxData/SwingArc` (`Units / -1`) остался фолбэком на случай тела без
группы.

**Глазами не проверено** — это к Максу, в живом стенде.

## Дальше — громкость блума

Макс (07.08): «Я все смотрю пока и настраиваю, но мне нужны реальные эффекты, чтобы дать итоговое
мнение XD». То есть шов и был тем, что его разблокировало. Лестница яркости (семь множителей в
коридоре 0.8–2.2, вспышка смерти самая тусклая) — после его вердикта, не до.
