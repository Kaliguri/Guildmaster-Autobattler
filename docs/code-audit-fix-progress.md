# Журнал починки по аудиту — 2026-07-26

Спутник [плана починки](code-audit-fix-plan.md). Здесь — что реально сделано, что отложено и почему, плюс находки, которые **устарели** ещё до начала работ.

## Важное про свежесть реестра

Аудит собирался два часа, и всё это время параллельные сессии коммитили в ту же ветку. Часть находок закрылась чужими руками **ещё до того, как отчёт был написан**. Поэтому правило работы: **сверять находку с живым кодом перед правкой**, а не идти по реестру вслепую.

Устаревшее, найденное при сверке:

| Находка | Что с ней |
|---|---|
| Всё про `AffinityTable` (CD-21, TS-6, часть R1-03) | Матрица **вырезана** коммитом `3f738fc8`, affinity теперь несёт идентичность, а не резист |
| Второй владелец команды зрителя (`CombatPresenter._localViewerTeam`) | **Закрыто** в `71e0bd0a` (сессия ГДД): поле снято, `ILocalPlayer` через DI, «свой/чужой» = `IsAllyOfViewer` |
| AC-16 «976 КБ scratch-сцены висят в репо» | **Ложная**: `MaxSceneForTests.unity` не отслеживается git. Реальный остаток мельче — шаблон в `.gitignore` (`*Scene For Tests.unity`, коммит `96ccb31a`) не ловит имя без пробелов, поэтому файл вечно торчит в `git status` |
| Мёртвый ключ `_legacyBattleScene` в `CoreScene` (волна 2) | **Ушёл сам** при сохранении сцены: поле в коде удалил `3c3b53ea` |
| RL-11 «`RunSingleBattleAsync` — копия пайплайна, которую никто не зовёт» | **Неточна**: вызывающий есть — `GameBootstrap` (dev-разрез по флагу `_runBattleFlowOnBoot`) |

---

## Сделано

### Волна 1, пункт 1 — дверь миграции (`38d20e6f`)
Удалён `Phase4Package3StatsBaseMigration`: живой `[MenuItem]`, чей `Run()` неидемпотентно переписывал `StatsConfig` и все реликвии к устаревшей базе (`MaxHP` 120 против 2000 у классового якоря), через `ApplyModifiedPropertiesWithoutUndo` + `SaveAssets` — то есть без Ctrl+Z и сразу на диск.

Докстринг запрещал удаление «по правилу 0.8 (история миграций)». Правила с таким номером **нет нигде в репозитории**, и документа с ним не было ни разу в git-истории. Историю миграций хранит git.

**Осталось:** две соседние двери — `Phase4Package1Migration` и `Phase4Package3AiPresetMigration`. Обе с `[MenuItem]`, `SaveAssets()` и той же висячей отсылкой. Не тронуты: просили закрыть одну.

### RC-1 — конфигурация, которую получает игрок (`2723870c`)
- `CoreScene` (единственная сцена в билд-листе) получила `_statsConfig`, `_classBalanceConfig`, `_actConfig` — стояли `{fileID: 0}` при заполненных тех же полях в `CombatSystemsScene`. `RootLifetimeScope` отдаёт первые два прямо в `UnitStatPreview` без фолбэка, то есть панель инвентаря считала статы от `null`-конфига.
- Инвариант «ТОТ ЖЕ ассет, что в `CombatLifetimeScope`» переехал из `[Tooltip]` в тест: `SceneWiringTests` обходит каждую включённую сцену билда, требует обязательные ссылки и сверяет, что общие конфиги — **один и тот же ассет** во всех сценах.
- **Красный прогон сделан:** при `_statsConfig: {fileID: 0}` на диске тесты падают с «CoreScene / RootLifetimeScope._statsConfig не назначено» и «_statsConfig расходится между сценами: CoreScene=NULL, CombatSystemsScene=StatsConfig». Тест умеет падать — это проверено, а не заявлено.

**Осталось по RC-1:** свести регистрацию к одной в Root (T-1, шаги 2-4), `_campScreen` (контентное решение по Лагерю), job сборки плеера в CI.

### RC-9 — работа, у которой нет хозяина (`e2830728`)
- **Атомарная запись сейва.** Было `File.WriteAllText` поверх единственного слота — прерывание уничтожало забег. Стало: временный файл → атомарная подмена, прежняя версия остаётся в `.bak`.
- **Битый сейв больше не притворяется отсутствующим.** Оба случая давали `default`, при этом `Exists()` отвечал «сейв есть» — поэтому «Продолжить» оставалась на экране и молча не работала. Теперь нечитаемый файл уезжает в `.corrupt` (данные игрока не удаляются), пробуется `.bak`, и `Exists` говорит правду.
- **Петля игры под защитой.** Вся игра жила в одном `UniTaskVoid` без `try`: любое не-cancellation исключение убивало её навсегда и молча (выглядит как зависшая игра). Теперь падение видно в логе, awaits привязаны к жизни объекта, петля поднимается с главного меню дважды, прежде чем сдаться вслух.
- **Подписки на отмену не переживают экраны.** `UiNavigator` выбрасывал каждый `CancellationTokenRegistration`; токен обычно — токен забега, поэтому закрытый экран оставался достижим до конца акта, а число подписок росло на каждый показанный экран. Теперь регистрации held per screen и снимаются на `Pop`/`PopAll`/`Remove`/`Dispose`.
- **Тесты:** у дискового бэкенда их не было вообще — семь тестовых файлов подставляют `ISaveService` в памяти по ссылке, поэтому сериализацию, повреждение и танец с временным файлом не видел никто. Добавлены `JsonFileSaveServiceTests` (реальный диск) и пять тестов навигатора, включая прогон двадцати экранов на одном токене забега.

### T-12 / T-13 — цвет HP и щита (`a13ed316`, частично уехало в `0a8ccbd9`)
Владельцев было четыре, и они **уже разошлись**: палитра держит щит `(0.62, 0.86, 1.0)`, а `CombatPresenter` своим полем красил цифры урона по щиту в `(0.4, 0.7, 1)`.

- `CombatPresenter._shieldColor` и `DefaultHealthColor(...)` удалены, палитра стала обязательной и единственной.
- `HealthBarView._fallbackHpColor` / `_fallbackShieldColor` удалены (третье место после SO и префаба).
- `_colorPalette` добавлен в реестр обязательных ссылок `SceneWiringTests`.

**Готча процесса:** правки в `CombatPresenter.cs` и `HealthBarView.cs` уехали в **чужой** коммит `0a8ccbd9` (аудио-сессия сделала `git add` по этим файлам, пока мои изменения лежали незакоммиченными). Код в игре и зелёный, но в моём коммите остался только тест. История не переписывалась: коммит чужой и смешан с их работой. Это ровно та готча про общий индекс при параллельных агентах — коммитить надо сразу, а не копить.

Тот же коммит переключил `SceneWiringTests` на **preview-сцены**: обычные `OpenScene`/`CloseScene` внутри EditMode-теста трогают то, что открыто у разработчика, и закрытие активной сцены подвесило Test Runner так, что понадобилась перезагрузка домена. Preview-сцены изолированы, прогон стал втрое быстрее.

### T-10 / R1-48 / R1-72 / R1-74 — каскад статов и атак/сек (`80f75450`)
Балансный аудитор — тот инструмент, по чьим таблицам принимаются решения о числах — читал юнита **иначе, чем игра**: строил `new Stats(config)` + модификаторы персоны, полностью пропуская `ClassBaseline` и `EnemyScalers`. То есть каждый `MaxHP` и `MoveSpeed` в отчёте принадлежал каскаду, которого в симе нет. Панель инвентаря и Content Hub при этом гоняли полный каскад, каждый из своей копии кода.

- Каскад теперь один — `EffectiveStats` в сборке боя, в том же порядке, что делает `RuntimeUnitFactory`. Превью, хаб и аудитор зовут его.
- «Атак/сек» так же: две квантизованные копии через `AttackTiming` и третья в аудиторе — вообще без квантизации, по сырому `AttackSpeed` (числа, которого 30 Гц не производит: 1.4 и 1.5 обе дают 20 тиков). Формула переехала в `AttackTiming`, копии удалены, тесты перенаправлены.
- Заодно снят кламп `AttackSpeed` в аудиторе: он применял `Min/Max` из `StatsConfig`, которых сим **не применяет** — таблица докладывала потолок, которого в игре нет. Существовать ли этому клампу — отдельный вопрос (T-18); враньём было докладывать его как факт.

### T-4 — пауза (`75b10e97`, тесты `85f336c9`) — **находка переформулирована**

Реестр записал «на паузе ли бой» как один факт с семью владельцами и предписал свести всё к `TimeScaleService`. **Так делать нельзя** — это два разных факта с похожими именами:

- `CombatSimulation.SetPaused` — «сим заморожен **сценарием**» (расстановка, передышка, полигон). Детерминированная: вступает со следующего тика, счётчик тиков идёт, пока в очереди команды.
- `TimeScaleService.SetPaused` — **пауза игрока**: обнуляет `Time.timeScale`, а с ним и `Time.deltaTime`, из которого `CombatLoopService` копит тики, — то есть сим встаёт через неё сам.

Слить их — сломать расстановку. Все вызовы, кроме тумблера Space, ставят только sim-паузу, и намеренно.

Настоящих дефектов было два:
1. **Тумблер читал состояние у сима, а применял к обоим**, при том что `ResetBattle` сбрасывает `_isPaused` сам. После любого сброса они расходились: Space размораживал расстановку и оживлял отряд вне боя, а после рестарта — ставил паузу вместо того, чтобы снять. Теперь тумблер читает и пишет одного владельца и сима не касается вовсе (заодно `CombatSimulation` ушёл из зависимостей контроллера — стал мёртвым).
2. **Пауза игрока переживала узел.** Боевой скоуп в persist-мире не разрушается, поэтому `Dispose()` (возвращает `timeScale` к 1) не звался, а `Reset()` намеренно хранит выбор игрока для dev-рестарта. Пауза, взятая перед последним ударом, держала мир замороженным всю передышку. Снимается теперь в трёх точках: возврат мира, рестарт боя, подготовка следующего.

Различие задокументировано у самого `CombatSimulation.SetPaused` и закреплено четырьмя тестами — включая тот, что запрещает `Reset()` трогать паузу игрока.

### RC-3 — дефолты кода против ассетов (частично)

Корень большой (~18 позиций); закрыты четыре, включая обе срочные.

**T-5 — `ActConfig` больше не переписывает сам себя (`e7e43eb7`).** `MapGenConfig.Validated()` клампил собственные поля и возвращал `this`, а этот POCO — сериализованное поле ассета на диске; генератор зовёт его на КАЖДУЮ генерацию карты. Два аудитора спорили: один написал «живая порча данных», другой — «ассет, на который никто не ссылается». Правы оба, и важен был порядок: порча стала достижимой ровно в тот момент, когда я подключила ассет в `CoreScene` тремя коммитами раньше. Теперь `Validated()` возвращает клампнутую копию (`ZoneRule`/`AnchorRule` — структуры, клон массива отвязывает полностью). Два теста: чтение не мутирует источник; два чтения ассета дают равные, но разные экземпляры.

**T-2 — снят третий владелец каждого значения тюнинга (`1da7410d`).** Guard-тест сравнивал **ассет** с `SimTuning.Default`, они совпадали — а расходился третий: C#-инициализаторы `SimTuningConfig`, где радиус тела стоял `0.575` против играемых `0.3`. Радиус тела задаёт и дистанцию расталкивания, и дальность каждой атаки через `CombatPositioning.AttackReachCenter`, так что созданный из меню конфиг сместил бы всем дальность, хотя «никто ничего не менял». Все 14 инициализаторов теперь читают `SimTuning.Default`; тултип перестал цитировать 0.575. Новый тест строит свежий конфиг через `CreateInstance` — случай, которого старая страховка структурно не видела, потому что грузила только уже существующий ассет.

Ранее в этом же корне: **T-10** (каскад статов и атак/сек, `80f75450`) и **T-12/T-13** (цвета, `a13ed316`).

**Осталось в RC-3:** T-11 (база `MaxHP` 1200 против 2000 — трогает баланс, нужен вердикт Макса), T-8 (`GameConfig` несёт 6-7 полей из 21), T-9 (`?: 0.4f` в `UnitView`), T-7 (цвета `MapStyle` против USS-токенов), T-17 (`_defaultLocale`), T-18 (существует ли кламп скорости атаки — вердикт), T-24 (`ContentPaths` против `ContentDomains`), T-27 (`GameplaySettings.Defaults`), R1-50 (`relic.base` захардкожен в пяти файлах), R1-34 (feel-фолбэки в четырёх файлах), UA-21 (палитра скопирована в `PaletteRemapWindow`), R1-78 (`ArcanaTitle`), TS-17 (`ArmorK` в 17 тестовых файлах), LT-11 (три форматтера статов).

---

## Отложено сознательно

| Что | Почему |
|---|---|
| Волна 5 (вендорные паки, ~500 МБ) | Решение Макса: отдельной веткой, не в этот заход |
| Контентные вырезания волны 0 (Сосуды, Предметы, четыре контент-типа, Лагерь) | Решение Макса: дизайнерские вердикты за ним, приношу списком |
| `IsAllyOfViewer`: фолбэк `_localPlayer != null ? … : 0` | Этот код только что правила сессия ГДД (`71e0bd0a`); не лезу следом |
| Правка `relic.base` Override 1200 (AC-6) | Меняет силу стартового юнита — это баланс, не техника |
