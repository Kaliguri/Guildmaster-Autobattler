{
  "permissions": {
    "allow": [
      "Bash(dotnet build *)",
      "Bash(dotnet test *)",
      "Bash(dotnet run *)",
      "Bash(dotnet format --verify-no-changes *)",
      "Bash(powershell *)",
      "Bash(./scripts/run-tests.ps1)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(*git *)",
            "command": "python \"${CLAUDE_PROJECT_DIR}/.claude/hooks/deny-broad-staging.py\"",
            "statusMessage": "Проверяю, что стейджинг точечный",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(*git commit*)",
            "command": "grep -q 'git commit' && { git show --name-only --format= HEAD | grep -q '^Assets/_Project/' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Журнал решений: коммит тронул Assets/_Project. Была развилка (выбор из двух и более), грабли или новый инвариант? Если да — новый файл docs/wiki/tech/00-meta/journal/ГГГГ-ММ-ДД-slug.md по шаблону из CLAUDE.md, раздел «Где живёт правда». Существующие записи не читать. Если ни одного да — ничего не писать, git и так помнит.\"}}'; } || true",
            "timeout": 10
          },
          {
            "type": "command",
            "if": "Bash(*git commit*)",
            "command": "python \"${CLAUDE_PROJECT_DIR}/.claude/hooks/remind-gdd-sync.py\"",
            "statusMessage": "Проверяю, не разошлась ли карточка ГДД",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
